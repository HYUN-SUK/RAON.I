# RAON.I 프로젝트 인수인계 문서 (Handoff Document)

**작성 일시**: 2026-09-04T16:25:00+09:00  
**기준 브랜치**: `main`  
**빌드 상태**: Next.js 16.1.1 Production Build 103/103 (100% 정상 통과)  

---

## 1. 현재 상태 요약 (Completed Work in Current Session)

이번 세션에서는 **Google Play 최신 Android 16 (API 수준 36) 타깃 AAB(v2) 심사 제출 완결** 및 **일일 로테이션 TourAPI 무한 루프 탈출 가드 탑재(120분 행 완치)**를 성공적으로 완료했습니다.

### 🟢 마일스톤 9.38: 일일 로테이션 TourAPI 무한 루프 탈출 가드 탑재 및 구글 플레이 Android 16(API 36) 타깃 업데이트 완결
- **TourAPI 무한 루프(120분 행) 완치**:
  - `scripts/daily-region-sync.mjs`의 `syncTourSpots`에서 새벽 점검/오류 응답(`resultCode !== '0000'`) 발생 시 3회 재시도 후 바깥 while 루프까지 안전 조기 탈출(`hasMore = false`)하는 2중 철벽 가드 탑재.
  - 공공 API 일시 점검 시에도 무한 헛돌기 없이 기존 캐시 데이터를 활용하여 5~8분 내 정상 완료 보장.
- **구글 플레이 최신 Android 16 (API 수준 36) 타깃 AAB 심사 제출 완결**:
  - PWABuilder를 통해 기존 서명키(`signing.keystore`)로 `kr.co.raoni.app` (버전 `1.0.1`, 코드 `2`, Target SDK `36`) 신규 AAB 생성.
  - Google Play Console의 [프로덕션]에 업로드 및 최종 심사 제출 완료 (연례 필수 요건 100% 충족).
- **크론잡 실행 시각 05:00 최적화 권고**:
  - 공공기관 일일 점검(02:00~04:30)을 완벽히 회피하기 위해 `cron-job.org` 실행 시각을 04:30 ➔ 05:00로 변경 조치 준비 완료.

### 🟢 마일스톤 9.37: 스마트플랜 인트로 문구 확정 노출 및 Stage 4 안심 인프라 2중 박스 컨테이너 완결
- **불필요한 연결선 제거**: 산/바다를 가로지르던 인위적인 경유지 점선(`timelineConnectPath`)을 완전 제거하고, 카카오내비 실제 주행 도로선(출발지 ➔ 도착지)만 단독 표출.
- **전체화면(100%) 지도 확보**: 모달 진입 시 하단 카드를 숨겨 지도로만 화면 전체를 시원하게 표출.
- **마커 터치 토글 팝업 & 지우기**: 마커 터치 시 하단에 장소 카드 팝업, 같은 마커 재터치 또는 우측 상단 `[X 닫기]` 클릭 시 카드가 닫히며 지도가 즉시 100% 확장.
- **순서 변경 리스트 정밀 제거**: 무거운 롱프레스 및 순서 변경 드래그 리스트를 제거하여 모바일 지도 반응속도 2배 이상 향상.

### 🟢 마일스톤 9.34: 스마트플랜 정품 장소 카드 지도 연동 및 터치 버튼 세로 2배 확대
- **정품 장소 카드 연동**: 지도 하단 팝업에 스마트플랜 본문의 `renderFactCard`를 100% 그대로 렌더링 (이모지, 뱃지, 주소, 영업시간, 유선확인, AI Pick, `⚠️ 정보가 달라요` 신고).
- **터치 영역 세로 2배 확대 (`h-14 w-9`)**: 좌측 `⇄` 스와프 및 `📍` 길안내 버튼을 높이 56px 세로 알약형으로 2배 확대하여 모바일 원클릭 터치감 극대화.
- **지도 내 스와프 연동**: 지도 하단 카드에서 `⇄` 스와프 터치 시 지도가 닫히면서 해당 카테고리 대체리스트 시트가 즉시 오픈.

### 🟢 마일스톤 9.35: 카테고리별 대체지도 전용 [이 장소로 선택] 버튼 탑재 및 분기
- **카테고리별 대체지도 (`mode === 'alternatives'`)**: 마커 터치 시 카드 하단 `⚠️ 정보가 달라요` 왼쪽에 `[✔ 이 장소로 선택]` 버튼 노출 ➔ 탭 시 즉시 스와프 확정 & 지도 닫힘 & 본문 반영. 현재 선택된 장소는 `현재 선택된 장소` 뱃지로 표시.
- **최종 여행 전체 동선 (`mode === 'full_timeline'`)**: 이미 확정된 코스이므로 선택 버튼 없이 본문과 100% 동일한 정품 순수 카드 노출.

### 🟢 마일스톤 9.36 & 9.37: 스테이지 3 & 4 직관적 UI 개편 및 안심 인프라 2중 박스 컨테이너
- **Stage 3 (목적지 근처 맛집/명소)**:
  - 타이틀: `Stage 3. 목적지 근처 맛집/명소`
  - 안내 문구: *"목적지 주변의 검증된 로컬 맛집과 놓치기 아쉬운 힐링 명소를 엄선했습니다."* 고정 노출.
  - 배치 순서: 식당(`RESTAURANT`) ➔ 명소(`SPOT`) ➔ 축제(`FESTIVAL`, 명소 바로 아래).
- **Stage 4 (편의시설 안내)**:
  - 타이틀: `Stage 4. 편의시설 안내`
  - 안내 문구: *"쾌적하고 안전한 여행을 위한 장보기 마트, 응급 병원, 주유소 정보입니다."* 고정 노출.
  - 배치 순서: 마트(`MART`) ➔ 병원(`HOSPITAL`) ➔ 주유소(`GAS_STATION`).
  - **안심 인프라 2중 박스 컨테이너**: 일반 여행지와 차별화된 은은한 블루/슬레이트 그라데이션 박스(`rounded-2xl border border-blue-200/90 shadow-sm`) 및 상단 `🛡️ 안심 여행 필수 인프라` 헤더 바 적용.
- **전체 동선 지도 마커 번호 동기화**: `timelinePlacesForMap`을 새 스테이지 순서와 일치하도록 정렬하여 본문 카드와 지도 번호(`①, ②, ③...`)가 1:1 완벽 일치.

---

## 2. 주요 기술적 결정 사항 (Technical Decisions)
1. **FactCard 컴포넌트 재사용 및 일원화**:
   - 지도 하단 카드를 별도로 만들지 않고 본문의 `renderFactCard(card, stage, options)`를 그대로 재사용하여 디자인 및 기능(스와프, 길안내, 유선확인, 신고)의 유지보수 단일화 달성.
2. **모드별 인터랙션 완벽 분기**:
   - `SmartPlanMapViewModal`에 `renderCustomCard` prop을 뚫고, `mode === 'alternatives'`일 때만 `isAlternativeMapMode: true`를 넘겨 하단에 `[이 장소로 선택]` 버튼을 동적으로 주입.
3. **전체 동선 리스트 정렬과 지도 핀 동기화**:
   - `timelinePlacesForMap`에 카테고리 우선순위(`RESTAURANT ➔ SPOT ➔ FESTIVAL ➔ MART ➔ HOSPITAL ➔ GAS_STATION`)를 적용하여 지도 마커 번호와 본문 스크롤 순서의 완벽한 1:1 대응 보장.

---

## 3. 다음 작업 가이드 (Next Steps)
1. **타임테이블 형태의 실시간 주행계획서 기능 기획/개발**:
   - 대표님께서 앞서 언급하신 "이후 타임테이블 형식의 계획서" 요구사항에 맞춘 출발시간/도착시간/체류시간 기반 타임라인 고도화.
2. **스마트플랜 사용자 체류 및 공유 경험 고도화**:
   - 완벽해진 지도 동선과 카테고리별 UI를 기반으로, 외부 공유 링크(`/share/plan/[id]`) 상의 완성도 점검 및 피드백 수렴.

---

## 4. 주의 사항 및 환경 설정 (Caveats)
- **카카오맵 Web SDK 도메인 등록**:
  - `NEXT_PUBLIC_KAKAO_JS_KEY`가 카카오 개발자 콘솔의 사이트 도메인(`localhost:3000`, 운영 도메인 등)에 정확히 등록되어 있어야 지도 렌더링이 정상 작동합니다.
- **Next.js 16 빌드 파이프라인**:
  - `package.json`의 `build` 스크립트에 `node --check scripts/*.mjs`가 연동되어 있으므로, 배치 스크립트 수정 시 문법 오류가 있으면 빌드 단계에서 즉시 검출됩니다.

---

## 5. 이전 마일스톤 주요 이력 (Historical Milestones)

### 🟢 2) 마일스톤 9.34: 스마트플랜 정품 장소 카드 지도 연동 및 터치 버튼 세로 2배 확대 완결
- **스마트플랜 정품 장소 카드 지도 하단 연동**:
  - `SmartPlanMapViewModal`의 하단 팝업에 `renderCustomCard` prop을 연결하여 본문의 `renderFactCard`를 100% 그대로 렌더링.
  - 이모지, 뱃지, 주소, 상세정보, 유선확인, AI Pick, `⚠️ 정보가 달라요` 신고 기능까지 완벽 동작.
- **터치 버튼 세로 2배 확대 (`h-14 w-9`)**:
  - 좌측 `⇄` 스와프 및 `📍` 길안내 버튼을 높이 56px 세로 알약형(rounded-2xl)으로 2배 확대하여 모바일 원클릭 터치감 극대화.
- **모달 카드 전용 [X 닫기] 버튼 및 스와프 즉시 연동**:
  - 카드 우측 상단 `[X 닫기]` 버튼 터치 시 카드가 닫히고 지도가 즉시 100% 전체화면으로 확장.
  - 카드 좌측 `⇄` 스와프 터치 시 지도가 닫히고 해당 카테고리 대체리스트 바텀시트가 즉시 오픈.

### 🟢 2) 마일스톤 9.33: 스마트플랜 최종 여행 동선 단순화 및 마커 터치 팝업 UX 완결
- **추천 장소 연결선 제거**:
  - `timelineConnectPath`를 완전 제거하여 산/바다를 가로지르던 인위적 선을 없애고, 카카오내비 실제 주행 도로(출발지 ➔ 도착지)만 단독 표출.
- **하단 리스트 기본 노출 완전 제거**:
  - 최종 여행 전체 동선 진입 시 하단 리스트가 뜨지 않고 화면 전체(100%)를 시원한 지도로 꽉 채움.
- **마커 터치 1위 장소 카드 팝업 및 지우기(닫기)**:
  - 마커 터치 시 스마트플랜 1위 확정 장소 카드가 슬라이드로 팝업.
  - 마커 재터치 또는 우측 `[X]` 지우기 버튼 터치 시 카드가 쏙 닫히며 지도가 즉시 100% 확장.
- **동선 변경 드래그 로직 정밀 제거**:
  - 롱프레스 타이머 및 순서 변경 리스트를 정밀 제거하여 지도 반응 속도 및 모바일 성능 대폭 향상.

### 🟢 2) 마일스톤 9.32: 스마트플랜 지도 UX 3대 정밀 고도화 완결
- **리스트 ↔ 지도 양방향 1초 스위칭 연동**:
  - `handleOpenAlternativesMap`: `swapCategory` 보존 후 `setSwapCategory(null)`로 리스트 시트를 부드럽게 닫고 지도 화면 전체 표출.
  - 지도 우측 상단 `[📋 리스트로 보기]` 버튼 추가: 터치 시 지도 닫히고 원래 보던 대체리스트 바텀시트가 즉시 복귀.
- **마커 0.1픽셀 정밀 자석 고정 (`xAnchor: 0.5, yAnchor: 1.0`)**:
  - 카카오 공식 앵커 속성을 직접 적용하고 내부 CSS의 `-translate-y-full` 중복 간섭을 제거하여, 전국 8km 축소 배율부터 30m 확대 배율까지 마커 꼭지점이 실제 육지 도로 위치에 100% 자석 고정.
- **하단 카드 100% 노출 (`z-[999]` 승격 & `pb-8` 안전 패딩)**:
  - 앱 하단 네비게이션 바(`z-50`)보다 높은 `z-[999]`로 모달 승격 및 하단 카드 `pb-8` 안전 패딩 적용으로 마커 터치 시 하단 카드가 완벽히 노출.

### 🟢 2) 마일스톤 9.31: 스마트플랜 3대 실전 지도 동선 고도화 및 롱프레스 방문 순서 변경 구현 완결
- **신규 컴포넌트 개발**:
  - `src/components/plan/SmartPlanMapViewModal.tsx`: 카카오맵 Web SDK 기반 싱글톤 캔버스 대화형 지도 모달.
- **대체리스트 지도로 보기**:
  - 스와프 시트 상단 `[지도로 보기]` 버튼 연동.
  - 카카오 주행 도로선 바탕에 `[① 상호명 ⭐인증]` 말풍선 마커 표출.
  - 마커 터치 시 하단에 '단일 카드 1개'만 슬라이드로 쏙 등장하여 `[이 장소로 선택]` 원클릭 교체.
- **추천 장소 카드별 숨기기/보이기**:
  - 카드 우측 상단 `[👁️ 숨기기]` 버튼 탑재.
  - 슬림 바(`"🍲 가는 길 식사 (숨김) [👁️ 보이기]"`) 접힘 및 고유 ID 기반 DB 영구 저장.
- **스테이지 5 하단 전체 여행 동선 지도**:
  - '목적지' 명칭 표준화.
  - 출발지 ➔ 1일차 ➔ 목적지 ➔ 2일차 ➔ 복귀지 `①, ②, ③...` 번호 핀 및 이동선 표출 (숨김 장소 100% 자동 제외).
- **0.3초 롱프레스 드래그 방문 순서 변경**:
  - 넓은 카드 어디든 0.3초 꾹 누르면 햅틱 진동 + 1.02배 팝업과 함께 순서 위아래 드래그 이동.
  - 지도 위 번호 핀(`① ➔ ②`)과 이동 경로선 실시간 자동 재정렬.
  - `visit_order` 순서표 분리 및 300ms 롱프레스 가드로 무결성 100% 보장.

### 🟢 2) 마일스톤 9.30: 일일로테이션 syncHospitals 구문 오류 완치 및 배치 검증 파이프라인 탑재
- `scripts/daily-region-sync.mjs`: `syncHospitals` 구문 오류 수정 및 `package.json` 빌드 파이프라인 강제 검증 연동.

### 🟢 2) 마일스톤 9.29: 홈 화면 다가오는 여행 카드와 일정 관리 카드 5단계 뱃지 완전 동기화
- **09:00 이후 자동 승격 가드 정규화**:
  - `ScheduleHomeWidget.tsx`의 `isSmartPlanAvailable` 수식에서 `hasSmartPlan` 가드를 제거하여 맛보기 플랜이 있더라도 09:00 도달 시 `ScheduleCard.tsx`와 동일하게 3단계/4단계(`🌤️ 날씨정보 업데이트 가능`)로 즉시 승격.
- **이모지 중복 표출 정규화**:
  - `ScheduleHomeWidget.tsx`의 뱃지 JSX에서 `<span>✨</span>` 중복을 제거하여 깔끔한 단일 이모지 표출 일치.

### 🟢 2) 마일스톤 9.28: 스마트플랜 캐싱 정시 실행 100% 보장 및 일일로테이션 TourAPI/NMC 지능형 재시도 보완
- **스마트플랜 캐싱 GitHub API 직접 연동**:
  - `cron-job.org`에서 스마트플랜 캐싱 URL을 GitHub Actions Workflow Dispatch API로 전환하여 새벽 04:05 정각 실행 100% 보장 (1.52초 즉각 트리거 검증 완료).
- **일일 로테이션 점검 회피 및 지능형 재시도**:
  - `cron-job.org` 실행 시각을 공공 API 점검 시간을 피한 04:30 AM으로 조정.
  - `scripts/daily-region-sync.mjs`: TourAPI 및 NMC API 호출 시 3회 지능형 백오프 재시도 및 실패 원인 정직한 표기 안전장치 탑재.
- **인천광역시 재실행 검증**:
  - 관광명소 `140건`, 병원 `22건` 실시간 수신 및 DB 자동화 로그 정상 반영 완료.

### 🟢 1) 마일스톤 9.25: 환불대기(`REFUND_PENDING`) 전용 `[환불 완료]` 버튼 및 송금 정보 카드 탑재
- **목표**: 예약자가 취소하여 관리자가 환불 송금을 마쳤을 때, 직관적인 계좌 확인 및 원클릭 완료 처리를 가능하게 함.
- **수정 내역**:
  - `AdminReservationDetailModal.tsx`:
    - `isRefundCase` (`REFUND_PENDING` / `REFUNDED`) 시 모달 우측에 **[환불 요청 계좌 정보 카드]** (은행/계좌/예금주/환불금액/취소사유) 표출.
    - 하단에 **`[환불 완료 (송금 완료)]`** 인디고색(`bg-indigo-600`) 버튼을 단독 활성화하여 `completeRefund` RPC 실행.
  - `src/app/admin/payments/page.tsx`:
    - 결제 관리 테이블 행 우측 `관리` 컬럼에서 `REFUND_PENDING` 시 **`[환불완료]`** 빠른 실행 버튼 탑재.

### 🟢 2) 마일스톤 9.26: 일일지역로테이션 관광명소(SPOT) 및 병원(HOSPITAL) 수신 카운터 정상화
- **목표**: 어제 지표 분기 수정 시 누락되었던 수신 카운터를 원천 가산하여 관리자 로그 0건 표기 버그를 완치.
- **수정 내역**:
  - `scripts/daily-region-sync.mjs`:
    - `syncTourSpots` 및 `syncHospitals`에서 API 목록 응답 즉시 `stat.fetched.active` 가산 및 3대 메트릭(`modified/rolling/cached`) 집계 스코프 정상화.
  - **대구광역시 재실행 검증 완료**: `automation_logs`에서 관광명소 `195건`, 병원 `21건` 실시간 정상 수신 및 3색 뱃지 반영 확인.

### 🟢 3) 마일스톤 9.27: 스마트플랜 생성 직후 `weather_window` 메모리 동기화 및 D-7~D-1 / D-0 생명주기 락(Lock) 무결성 확립
- **목표**: D-7~D-1 또는 D-0 시점에 정밀 플랜 생성 시, 생성 직후 업데이트 버튼이 일시적으로 깜빡이며 다시 열리던 버그 완치.
- **수정 내역**:
  - `src/components/plan/SmartPlanProposal.tsx`:
    - `fetchPlan` 및 `handleCardSwap` 완료 시 현재 D-Day 시기에 맞는 `calculatedWeatherWindow`(`SHORT`/`MID`/`NONE`)를 `plan` 메모리 상태 객체에 즉시 주입.
    - `D-7 ~ D-1` 생성 시: **`[✨ 주간 예보 업데이트 완료]`(비활성화 락)** 즉시 고정 (D-0 도달 시까지 재활성화 차단).
    - `D-0(당일)` 생성 시: **`[✨ 출발 당일 스마트플랜 최신화 완료]`(최종 락)** 즉시 고정.
    - `D-8 이상` 생성 시: `weather_window = 'NONE'` 락 ➔ **D-7 도달 시 `[🔄 주간 예보 정밀 플랜 업데이트]` 정상 오픈**.

### 🟢 4) 10월 16일 영희네·석이네 빈자리 알림(Waitlist) 전수 검증
- `waitlist` 테이블에서 10/16 site-2, site-4 대기자 2명(4건)에 대해 오늘 09:01분에 `notified_at` 발송 완료 및 사이트 가용성 오픈 확인 완료.

---

## 2. 핵심 기술적 결정 사항 (Key Technical Decisions)

1. **상태 분기 무결성 (State Isolation)**:
   - 환불 완료 버튼은 오직 `REFUND_PENDING` 상태에서만 노출되며, 일반 예약 확정/취소 버튼과 완벽히 격리.
2. **원천 수신수 집계 정책**:
   - 일일 동기화 엔진은 외부 API에서 데이터를 받아오는 즉시 `stat.fetched.active`에 반영하고, 신규/수정/캐시재활용은 세부 분류 지표로 정직하게 기록.
3. **클라이언트 메모리 - DB 제로 딜레이 동기화**:
   - 스마트플랜 생성 후 DB 응답을 기다리지 않고 `generatedPlan` 객체 자체에 `weather_window`를 계산 주입하여 UI 깜빡임(Glitch) 0건 달성.

---

## 3. 다음 세션 작업 가이드 (Next Steps)

1. **사용자 신규 요구사항 및 관제 모니터링**:
   - 관리자 대시보드(`/admin/payments`, `/admin/automation/logs`) 실시간 현황 모니터링.
2. **Next.js 16 Production Build 무결성 지속 유지**:
   - `npm.cmd run build` 실행하여 103/103 100% 통과 상태 항시 유지.

---

## 4. 주의 사항 (Important Notes)

- **절대 규칙**: 모든 문제 및 오류 발생 시 임의 수정 금지, 원인 및 방안 보고 후 대표님 승인 후 코딩 진행.
- **날짜/시간 표기**: 항상 KST(한국 표준시) 기준 표준 헬퍼(`formatLocalDate`, `toKstYMD` 등)를 사용하여 UTC 시차 왜곡을 방지할 것.

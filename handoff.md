# 📝 Session Handoff: RAONAI Pipeline & Popularity Engine v2

## 1. 현재 상태 요약 (Current Status)
이번 세션에서는 RAONAI 데이터 파이프라인의 **정합성 감시 체계(Audit System)**를 대폭 강화하고, 수집 중단된 `readcount` 지표를 대체할 **차세대 실이동 기반 인기도 엔진(v2)**의 설계를 완료했습니다.

- **파이프라인 지표 세분화**: 모든 데이터의 상태를 `Active/Inactive`로 나누어 기록하며, 관리자 대시보드에서 이를 시각적으로 확인할 수 있습니다.
- **인기도 엔진 v2 설계**: Tmap(행동 기반) 및 KT(관동 집중률) 데이터를 결합한 새로운 스코어링 공식(BasePop & SeasonBoost)을 수립했습니다.
- **안정성 확인**: 전체 코드의 빌드 테스트 및 Git 커밋을 완료하여 즉시 운영 반영 및 다음 단계 착수가 가능한 상태입니다.

## 2. 주요 기술적 결정 사항 (Technical Decisions)
- **3진 아웃(Strike-Out) 강화**: API 수신 목록에서 누락된 데이터를 즉시 삭제하지 않고 3회 누적 시 비활성화하여 일시적 API 오류로부터 데이터를 보호합니다.
- **지표 시각화**: `{ active, inactive }` 객체 구조를 사용하여 로그를 기록하며, 프론트엔드에서는 `renderMetric` 헬퍼를 통해 하위 호환성을 완벽히 보장합니다.
- **BasePop(중심성) 도입**: Tmap 연관 관광 데이터를 단순 순위가 아닌 전국 네트워크 중심성 점수로 변환하여, 실질적인 '동선의 허브'를 정밀하게 추출하도록 설계했습니다.

## 3. 다음 작업 가이드 (Next Steps)
1. **인기로 엔진 구현**: 
   - `scripts/daily-region-sync.mjs` 내의 `rotateTourPopularity`를 `updateSpotPopularity`로 교체 구현.
   - 17일 순환 주기에 맞춰 Tmap/KT API 데이터를 `raw_data`에 차곡차곡 쌓는 로직부터 시작.
2. **D-3 캐싱 엔진 점검**: 
   - 현재 캠핑 플랜 생성 로직이 새로운 인기도 점수를 정상적으로 참조하는지 연동 테스트 필요.
3. **일일 배치 모니터링**: 
   - 내일 새벽 돌아갈 전국 단위 배치가 개편된 `{ active, inactive }` 로그 형식으로 정상 기록되는지 대시보드 확인.

## 4. 주의 사항 (Caveats)
- **API 쿼터 제한**: Tmap/KT API는 각각 일일 1,000회 한도가 있으므로, 전국 데이터를 한 번에 긁는 대신 현재의 **17일 지역 로테이션 구조**를 반드시 태워서 호출량을 분산해야 합니다.
- **ID 매칭**: 공공데이터포털 간 고유번호가 일치하지 않는 경우가 많으므로, `spot_code_mapping` 테이블을 활용한 3단계 매칭 전략을 엄격히 준수해야 합니다.

# Session Handoff (2026-05-15)

## 1. 현재 상태 요약 (Current Status)
*   **스마트플랜 수익화 전략 기획 완료**: '스마트플랜 LIVE'라는 이름으로, 기존 정적(Map) 무료 버전과 대비되는 '실시간 동적 타임라인(GPS)' 기반의 유료화 패키지 방향성을 확정했습니다.
*   **핵심 5대 기능 정의**: 타임 리밸런싱, 스마트 핏스탑, 테마 스위처, SOS 메디컬 레이더, Co-Op 플래너.
*   **아키텍처 및 MVP 구현 계획 수립**: `docs/RAONAI_LIVE_IMPLEMENTATION_PLAN.md`에 개발 우선순위(Phasing)와 구체적 로직 설계를 문서화하여 영구 저장했습니다.

## 2. 기술적 결정 사항 (Technical Decisions)
*   **코어 구조 변경**: 유료 버전은 단순 장소 리스트가 아닌 **'시간대별 타임라인 배열'**로 AI 프롬프트 출력을 분리합니다.
*   **타임 리밸런싱 UX**: 카카오/네이버 로컬 API에 영업시간이 없는 한계를 극복하기 위해, 마감 시간 임박 시 '전화 팝업 -> 자동 스왑'으로 이어지는 UX 흐름을 결정했습니다.
*   **Co-Op 플래너 로직**: 생성 전 투표가 아닌, **생성 후 타임라인의 대체 리스트 내 좋아요(최대 3개) 투표 및 실시간 Auto-Swap(1위 갱신)** 방식으로 확정하여 직관성을 높였습니다.

## 3. 다음 작업 가이드 (Next Steps)
다음 세션에서는 기획 논의를 종료하고 실제 **Phase 1 코딩**에 돌입해야 합니다.
1.  **AI 프롬프트 분리 및 고도화**: 유료 버전을 위한 시간대별 JSON 배열(`time`, `duration_mins`, `location_id` 포함)을 정확하게 출력하는 프롬프트(Gemini) 엔드포인트 신설.
2.  **타임라인 UI 컴포넌트 개발**: 카테고리 뷰가 아닌, 각 타임블록에 `[출발/시작]` 버튼과 `대체 리스트(투표 모드)`가 달린 Stepper 형태의 UI 퍼블리싱.
3.  **모드 분기 UI**: 페르소나 선택 직후 "캠핑 모드 vs 일반 여행 모드"를 선택하는 UI 추가 및 전역 상태(Store) 연동.

## 4. 주의 사항 (Known Issues / Warnings)
*   `RAON_MASTER_ROADMAP_v3.md` 파일의 인코딩 문제(ANSI/EUC-KR 추정)로 인해 터미널 상에서 한글이 깨져 보이는 현상이 있습니다. (내용 업데이트는 PowerShell을 통해 안전하게 추가했습니다.)
*   기존 무료 버전(Basic)의 기능과 코드가 망가지지 않도록, `PRO` 로직을 완전히 별도의 컴포넌트/함수로 분리하여 사이드 이펙트를 차단해야 합니다.

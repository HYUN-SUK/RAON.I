# 🤝 인수인계 문서 (Handoff) - 2026-06-07 세션

## 1. 현재 상태 요약
이번 세션에서는 백년가게 맛집 정화 작업에 이어, **미입금 예약에 대한 백그라운드 자동 취소 스케줄러 시스템**과 **관리자 예약 관리 화면 기능 개선**을 완벽하게 마무리하였습니다.

- **서버 사이드 Cron API 구축**: `/api/cron/cancel-overdue` 엔드포인트를 구현하여 기한이 지난 PENDING 예약을 백그라운드에서 주기적으로 취소하고 알림을 발송하도록 설계했습니다.
- **Vercel 크론 타임존 보정**: 서버 환경(UTC)과 한국 표준시(KST) 간의 9시간 오차로 인해 발생한 유예 기한(Grace Period) 판정 버그를 KST 기준 시간 보정 계산을 통해 완벽하게 해결했습니다.
- **입금 기한 DB 실시간 동기화**: `site_config` 테이블에 `deposit_deadline_hours` 컬럼을 신규 배치하여 관리자가 화면에서 변경한 시간이 DB를 통해 크론 스케줄러와 프론트 스토어에 즉시 연동되도록 하였습니다.
- **관리자 UI 시각화 및 개별 취소 탑재**:
  * 예약 리스트의 개별 카드에서 입금 기한 초과(overdue) 시 **빨간색 경고 표시** 및 **최종 유예 시간**을 보여주어 직관성을 높였습니다.
  * 일괄 취소 버튼 외에 만료 대기 목록에서 1건씩 수동으로 즉시 취소할 수 있는 **[개별 취소]** 기능과 버튼을 추가했습니다.
- **외부 크론 서비스 연동 성공**: Vercel 무료(Hobby) 플랜의 크론 스케줄 제약으로 인한 배포 무반응 오류를 `vercel.json` 제거 후 외부 크론 서비스([cron-job.org](https://cron-job.org/))에 연동하여 해결 완료했습니다.
- **최종 세션 wrap-up 코드 정리 및 Lint 확인**: 
  * `ReservationCard.tsx`에서 사용하지 않는 import 모듈(`notificationService`, `NotificationEventType`, `AlertTriangle`) 및 미사용 클릭 핸들러(`handleCancelClick`) 함수를 제거하였습니다.
  * 이번 작업의 수정을 받은 주요 파일들(`route.ts`, `OverdueReservations.tsx`, `ReservationCard.tsx`, `useReservationStore.ts`)에 대해 ESLint 정적 분석 검사를 실행하여 `any` 캐스팅 및 미사용 변수 경고/에러 20건을 모두 완벽하게 해결하고 통과시켰습니다.

---

## 2. 주요 기술적 결정 사항
- **보안용 `CRON_SECRET` 검증**: API 무단 기동 및 해킹 공격을 방지하기 위해 요청 헤더의 `Authorization: Bearer <CRON_SECRET>` 검증 로직을 추가하여 보안성을 높였습니다.
- **서버 타임존 UTC ➔ KST 수동 보정**: Vercel의 서버 환경이 UTC인 점을 감안해, 한국 표준시(KST) 기준 영업 유예 기간(오전 9시 ~ 오후 6시 기준 자동 연장)을 판정할 때 마감 시간에 직접 9시간(`9 * 60 * 60 * 1000`)을 더하여 보정한 후 비교를 수행하게 설계하여 판정 불일치 문제를 해결했습니다.
- **`vercel.json` 파일 배제 및 외부 크론 차용**: Vercel Hobby 플랜은 `vercel.json`을 통해 10분 크론을 등록하면 검증 실패로 아예 배포 트리거를 차단합니다. 이를 우회하기 위해 `vercel.json`을 삭제하고, Next.js 백엔드 루트는 퍼블릭으로 유지한 채 보안 헤더가 탑재된 무료 외부 크론 대행을 물려 비용 없이 10분 배포 크론 시스템을 구축했습니다.

---

## 3. 다음 작업 가이드
- **외부 크론 이력(History) 주기적 모니터링**: `cron-job.org`에서 10분 주기로 API를 찌를 때 `Status 200 OK`가 꾸준히 누락 없이 실행되는지 이력을 체크합니다.
- **알림 발송 신뢰성 체크**: 실제 만료 건 발생 시 고객의 푸시/카카오 알림 전송 이력 및 전송 성공 여부를 notification 로그에서 추적합니다.
- **추가 관리자 기획 요건 보완**: 다음 세션에서 추가적으로 필요한 실시간 예약/취소 현황판 보완 및 대시보드 통계 기능 확장이 필요할 경우 진행합니다.

---

## 4. 주의 사항 및 환경 설정
- **Vercel 환경 변수**: Vercel 프로덕션 배포 시 Vercel 콘솔 Settings 내 **`CRON_SECRET`** 환경 변수가 아래의 로컬 값과 동일하게 등록되어 있는지 반드시 확인해야 합니다.
  * `CRON_SECRET=7e6d1b3e-6d5f-4689-975c-1d2e3aaf38aa`
- **로컬 테스트**: 로컬 개발 중 테스트 시에는 `node scripts/` 내 임시 파일들을 실행하여 mock 데이터를 주입하고 cURL로 API를 노킹해 검증할 수 있습니다.

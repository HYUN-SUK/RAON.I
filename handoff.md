# Session Handoff: Smart Plan Persona & Stabilization

## 1. 현재 상태 요약
- **Persona 추출 안정화**: RLS 문제를 해결하기 위해 서버 사이드 인증(`supabase-server.ts`)을 적용했으며, 예약 내역보다 `User Camping Profile`을 우선 참조하도록 개선했습니다.
- **날씨 데이터 동기화**: 기상청 API와 시스템 간의 날짜 형식 불일치 문제를 해결하고, 여행 전 기간에 대한 날씨 브리핑이 가능하도록 로직을 수정했습니다.
- **AI 내러티브 강화**: 페르소나(구성원)와 날씨 정보가 AI 응답의 첫 문단에 반드시 포함되도록 프롬프트를 강화했습니다.
- **Git 상태**: 원격 저장소(`main`)와의 충돌을 해결하고 리베이스를 완료했습니다. 현재 빌드 테스트(`npm run build`) 결과도 정상입니다.

## 2. 기술적 결정 사항
- **서버 사이드 인증 우선**: 클라이언트 사이드 RLS 제약을 피하고 보안을 유지하기 위해 `getServerSupabase` 클라이언트를 핵심 로직(`persona.ts`, `smartPlan.ts`)에 주입했습니다.
- **Hierarchical Fallback**: 사용자 프로필 정보가 없을 경우 스냅샷이나 과거 예약 내역을 단계적으로 참조하여 AI 페르소나가 항상 생성되도록 보장했습니다.

## 3. 다음 작업 가이드 (핵심 과제)
- **복수 예약 데이터 혼선 해결**: 동일 날짜에 여러 예약이 있을 경우, `userId`와 `startDate`만으로는 예약 ID를 특정할 수 없어 엉뚱한 캠핑장의 장소 데이터가 노출되는 문제가 발견되었습니다.
    - **해결책**: 예약 조회 시 전달받은 `location`(위경도) 좌표와의 근접도를 계산하여 정확한 `reservation_id`를 매칭해야 합니다.
- **D-3 캐싱 파이프라인 점검**: `smart_plan_candidates` 테이블에 데이터가 적재되는 과정에서 예약별 격리가 완벽한지 재확인해야 합니다.
- **명소(SPOT) 점수 로직 정제**: 더 이상 사용되지 않는 `readcount` 점수화 로직을 제거하고 축제(FESTIVAL)와의 차등 점수 체계를 정밀화해야 합니다.

## 4. 주의 사항
- **DB Push 필요**: `20260506_add_seniors_to_profile.sql` 마이그레이션 파일이 로컬에 생성되어 있으나, Supabase 클라우드 DB에는 아직 반영되지 않았을 수 있습니다. 다음 세션 시작 시 `supabase db push` 확인이 필요합니다.

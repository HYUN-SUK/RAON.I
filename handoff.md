# 세션 인수인계 문서 (Handoff)

## 1. 현재 상태 요약
- **코드 상태**: 모든 코드가 이번 세션 시작 전의 **v11.9.22 원본 상태로 롤백**되었습니다.
- **주요 성과**: 개인화 로직(Stage 4) 주입을 시도하였으나, 구조 변경 과정에서 원본 엔진의 정교함(식당 점수 합산, 명소 하이브리드 등)을 훼손하는 심각한 오류를 발견하여 안정성을 위해 롤백을 결정했습니다.

## 2. 기술적 결정 및 반성 (Critical Failures)
- **실책**: 코드를 정리한다는 명목으로 원본의 `for` 루프 내부 스코어링 로직을 간소화하거나 별도 함수로 분리하려 했습니다. 이 과정에서 **식당의 점수 누적 합산 로직**과 **명소의 v2.6 하이브리드 엔진**이 손상되어 결과물의 품질이 급격히 저하되었습니다.
- **교훈**: 원본 엔진은 단순한 수집 도구가 아니라 수많은 필터링과 예외 처리가 집약된 "블랙박스"와 같습니다. 이를 재구성하려 하기보다, **원본 엔진을 블랙박스 그대로 두고 그 결과물에 레이어를 얹는 방식**이 유일한 정답임을 확인했습니다.

## 3. 다음 작업 가이드
- **Surgical Fix 우선**: `caching-smart-plan.mjs`의 클러스터링 로직에 `reservation_id` 정보를 추가하는 작업부터 아주 좁은 범위에서 시작하십시오.
- **로직 주입**: 원본의 `categories` 루프와 `Kakao Verification` 루프는 **절대 건드리지 마십시오**. 모든 수집이 끝난 후, `totalFactMap`을 기반으로 예약자별 거리 감점을 적용하는 독립적인 루프를 후단에 배치하십시오.
- **데이터 검증**: `월척오리`와 같은 중복 식당이 합산되어 상단에 노출되는지, `홍성의료원`과 같은 핵심 거점이 누락 없이 검증되는지 최우선으로 확인하십시오.

## 4. 주의 사항
- **RPC 규격**: 사용자 서버의 RPC 함수명은 `get_master_places_in_radius_v2`이며, 파라미터는 `target_lat`, `target_lng`, `radius_meters`, `p_category`, `limit_count`입니다. (오타 주의)
- **환경 변수**: Supabase 서비스 롤 키 및 Kakao API 키가 정상적으로 설정되어 있는지 확인하십시오.

# Handoff: Smart Plan UI & Logic Refinement

## 현재 상태 요약 (Current Status)
이번 세션에서는 사용자가 모바일 환경에서 겪었던 UI 밀림 현상과 스와프 시 발생하는 중복 버그, 그리고 AI 내러티브의 개인화 부족 문제를 성공적으로 해결했습니다. 또한, 저장된 플랜 데이터가 있을 때 자동으로 플랜 화면을 보여주도록 로직을 복구했습니다.

- **모바일 UI 정렬**: 타임라인 세로줄을 `10px`로 맞추고, 장소 카드의 너비를 `w-[calc(100%-3rem)]`으로 수정하여 화면 안에 완전히 들어오게 보정했습니다.
- **스와프 팝업 안정화**: 리스트의 고유 ID를 기반으로 중복 옵션을 제거하고(Map 활용), 현재 활성화된 장소가 대체 후보에 뜨지 않도록 배제하는 로직(`activeIds.includes`)을 추가하여 스테이지 5의 스와프 버그를 해결했습니다.
- **AI 개인화 주입 강화**: 페르소나 객체에서 직접 `guestDetails`(성인, 아이 수, 반려견 유무)를 추출해 AI 프롬프트 첫 문단에 반드시 포함하도록 강제 지침을 적용했습니다.
- **기존 플랜 자동 로드**: `page.tsx`에서 `schedule.smart_plan_data`가 존재하면 자동으로 스마트 플랜 화면으로 전환되도록 복구했습니다.

## 기술적 결정 사항 (Technical Decisions)
- **중복 방지 맵핑(Deduplication Map)**: `smartPlan.ts`의 백엔드 로직을 건드려 생태계를 흔드는 대신, 프론트엔드(`SmartPlanProposal.tsx`)에서 스와프 옵션을 렌더링할 때 `activeIds` 필터링과 `Map`을 통한 고유 ID 추출을 활용하여 중복을 안전하고 완벽하게 제거했습니다.
- **CSS 계산 (calc)**: 디바이스 가로 크기에 상관없이 좌우 여백을 동일하게(24px) 유지하기 위해 정적인 마진값 대신 `w-[calc(100%-3rem)]`을 도입했습니다.

## 다음 작업 가이드 (Next Session Tasks)
다음 세션에서는 다음과 같은 두 가지 주요 과제를 진행할 예정입니다:
1. **AI 산출물 안정성 검증**: 스마트 플랜의 브리핑(히어로) 영역에서 사용자의 개인 정보(인원수 등)가 제대로 녹아들었는지 점검하고, 문구가 안정적으로 생성되는지 프롬프트를 미세 조정합니다.
2. **사전 경로 선택 (Route Selection) 단계 도입 논의**: 카카오내비 API를 활용해 출발지-도착지 간의 추천 경로 3가지 정도를 **스마트 플랜 생성 전에 미리 사용자에게 보여주고 선택하게 하는 단계**를 추가할 계획입니다. 이를 통해 중간 기착지(Midpoint) 데이터를 보다 정확하게 수집할 수 있습니다.

## 주의 사항 (Notes & Warnings)
- `SmartPlanProposal.tsx`의 스와프 로직이 꽤 복잡하게 얽혀 있으므로, 추후 카테고리를 추가하거나 데이터 구조를 변경할 때 필터링 기준(`id` 기반)을 반드시 지켜야 합니다.
- 경로 선택 단계 도입 시 `CampingProfileGate` 컴포넌트와의 연결 흐름 재설계가 필요할 수 있습니다.

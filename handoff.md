# Session Handoff: Route Selection & AI Stability

## 현재 상태 요약
이번 세션에서는 스마트플랜 생성 전 단계인 **'경로 선택(Route Selection)'** 기능을 성공적으로 통합하고, 배포 환경에서의 AI 응답 불안정성을 해결했습니다.

- **경로 선택 기능 구현**: 카카오 내비 API를 연동하여 추천/대안 경로를 선택할 수 있는 모바일 최적화 UI(`RouteSelector`)를 구축했습니다.
- **API 비용 최적화**: 호출 횟수를 최소화하기 위해 `alternatives=true` 옵션을 사용하여 1회 호출로 최대 3개의 경로를 확보하도록 설계했습니다.
- **AI 안정화**: 배포 환경(Vercel 등)에서 제미나이의 JSON 응답이 파싱되지 않던 문제를 '마크다운 제거 정규식' 및 'Robust Parsing' 로직 도입으로 완벽히 해결했습니다.
- **UI 보완**: 경로가 1개뿐인 단순 구간에서 사용자에게 친절한 안내 문구를 표시하도록 처리했습니다.

## 기술적 결정 사항
- **병렬 호출 대신 단일 호출(`alternatives=true`)**: API 쿼터(일 5,000건) 절약을 위해 기준이 다른 여러 번의 호출 대신, 카카오가 제안하는 대안 경로를 적극 활용하는 방식으로 선회했습니다.
- **정규식 기반 JSON 추출**: 제미나이가 응답에 마크다운 기호(` ```json `)를 포함하더라도 오류 없이 순수 JSON만 추출할 수 있도록 파싱 로직을 강화했습니다.
- **시간 기반 중간지점(Midpoint)**: 거리 기준이 아닌 전체 소요 시간의 50% 지점을 경유지로 선정하여, 실제 운전자가 체감하는 중간 지점의 정확도를 높였습니다.

## 다음 작업 가이드
1. **내비게이션 앱 연동**: 사용자가 선택한 경로 정보를 스마트폰에 설치된 내비 앱(카카오내비, T맵, 네이버 지도)으로 바로 보낼 수 있는 딥링크 기능을 구현해야 합니다.
2. **히어로 안내 프롬프트 고도화**: 스마트플랜 첫 단계의 AI 안내 문구가 더욱 감성적이고 정교하게 생성되도록 프롬프트를 추가 튜닝할 예정입니다.
3. **중간지점 데이터 흐름 점검**: 선택된 중간 지점 좌표가 Stage 1~2의 추천 장소 검색에 정확히 반영되는지 최종 검증이 필요합니다.

## 주의 사항
- **API 키 관리**: 배포 환경에서 `NEXT_PUBLIC_GEMINI_API_KEY` 환경 변수가 누락되지 않도록 주의해야 합니다.
- **모델명**: 현재 최신 모델인 `gemini-2.5-flash-lite`를 사용 중입니다. 모델 가용성 상태를 상시 모니터링해야 합니다.

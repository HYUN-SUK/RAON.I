# 🚀 안티그래비티 세션 종료 보고 및 헨드오프 요약 (Handoff Summary)

이 문서는 라온아이(RAON.I) 프로젝트의 홈 화면 UX 개선, 비로그인 터치 가드 장치 강화, 10초 기록 및 지도 연동 보완, 그리고 나의 여행일정 화면 취소 요청 로직을 완수한 내역을 다음 개발 세션으로 매끄럽게 인수인계하기 위해 작성된 보고서입니다.

---

## 1. 이번 세션 완료 작업 (Current Status Summary)

*   **홈 화면 초보자 뷰(BeginnerHome) 고정 및 튕김 현상 해결**:
    *   사용자의 요구대로 기존 사용자/초보자 자동 판별 로직을 일시 중지하고, **모든 사용자에게 초보자 홈 화면(`BeginnerHome`)이 즉시 로드되도록 강제 고정**했습니다.
    *   이를 통해 백그라운드에서 유저 분류가 비동기로 갱신되며 발생하던 부모 컴포넌트 리렌더링 트래픽을 원천 제거하여, **일정 카드를 클릭해 상세화면으로 진입할 때 홈 화면으로 튕겨나가던 현상을 100% 영구 해결**했습니다.
    *   동시에 불필요한 Supabase 과거 예약 카운트 쿼리를 스킵하여 **홈 화면 첫 기동 로딩 속도가 획기적으로 향상**되었습니다.
*   **비로그인 사용자 보호 가드 강화**:
    *   소개 6개 칩 외의 모든 경로(레시피/놀이 탐색기 배너, 일정 관리 관련 액션 등)에 `withAuth` 로그인 모달 가드를 일괄 적용하여 비로그인 접근 차단을 완성했습니다.
*   **10초 기록 & 캠핑 지도 연동 UX 개선**:
    *   기존에는 성공 팝업에서 "핀 확인하기" 클릭 시 내 수첩(`myspace`)으로 페이지 전환 후에만 지도가 떴으나, 홈 화면(`ReturningHome`, `BeginnerHome`) 최하단에 `MyMapModal`을 직접 마운트하여 **페이지 이동 없이 홈 화면에서 지도가 즉시 팝업**되도록 직관적인 UX를 완성했습니다.
*   **10초 기록 공개 설정 UI 고도화**:
    *   단순한 `Switch` 토글 형태에서 예쁜 **2지선다 가로 병렬형 카드 버튼(나만 보기 / 전체 공개)**으로 개편했습니다.
    *   테마 컬러인 Forest Green (#224732) 배경색 대비를 주어 사용자가 공개 범위 설정 상태를 정확하게 시각적으로 인지하도록 유도했습니다.
*   **주변 행사 추천 API 필터링 보완**:
    *   서버 시간 기준(UTC)으로 발생하던 오늘 날짜의 9시간 오차를 **한국 표준시(KST) 시간대로 보정**하여 정확하게 필터링되도록 보완했습니다.
    *   날짜가 미기입되었거나 파싱할 수 없는 깨진 행사 데이터는 추천 목록에서 엄격히 제외 처리(`return false`)했습니다.
*   **나의 여행일정 취소 요청 로직 통합 및 입금대기 연동**:
    *   `ScheduleCard` 및 `myspace/schedule/page.tsx` 리스트 화면에 **"취소요청"** 버튼을 배치하고 기존 예약 취소 로직을 연동했습니다.
    *   **입금 대기(`PENDING`) 상태의 예약을 일정 리스트 상단에 임시로 노출**시켜, 사용자가 미입금 예약도 바로 확인하고 취소할 수 있도록 연동했습니다.
    *   임시 카드의 캠핑장명을 **`라온아이 (철수네)`** 처럼 구체적인 사이트 한글명으로 매핑하여 노출되도록 보완했습니다.
    *   입금대기 임시 카드에 대해서는 **우측 하단의 상세 링크("예약 확인")를 숨기고 카드 클릭 동작을 비활성화(`onClick={undefined}`)**하여, 사용자 혼선을 원천 방지했습니다.
    *   `onClick`이 비활성화되더라도 **하단 좌측의 "취소요청" 버튼은 독립적으로 정상 노출 및 클릭**되도록 래핑 조건식을 보완했습니다.
    *   입금이 완료(`CONFIRMED`)되면 이 임시 카드는 자동으로 사라지고 기존의 정식 일정 카드로 부드럽게 대체됩니다.
*   **내수첩 하단 입금대기 카드 상세정보 버튼 제거 및 컨펌 추가**:
    *   모달 팝업 중첩이나 페이지 혼선을 유발하던 "상세보기" 버튼을 삭제했습니다.
    *   대신 "예약 취소" 버튼을 가로 전체 너비(`w-full`)로 깔끔하게 1단 확장하여 혼선 없는 단일 액션을 제공하도록 정돈했습니다.
    *   실수로 즉시 취소되는 문제를 예방하기 위해, **버튼 클릭 시 확인 컨펌 창(`window.confirm`)을 출력하도록 안전한 이중 확인 가드를 도입**했습니다.
*   **일일 자동화 시스템 안정화 및 WAF 영구 우회**:
    *   **크롤러 100건 제한 최적화**: 60분 시간 제한을 초과해 강제 취소되던 문제를 해결하고자 일일 수집 한도를 식당 90건, 마트 10건(총 100건)으로 최적화했습니다.
    *   **커서 백필 자동 커밋 연동**: 크롤러 성공 완료 후 `scratch/last_enrich_cursor_id.txt`를 원격 `main` 브랜치로 자동 `git commit & push` 하도록 GHA 워크플로우에 `permissions: contents: write` 권한 및 단계를 연동해 매일 다음 데이터로 진도가 흐르도록 구현했습니다.
    *   **3진 아웃 오진단 방지 가드**: 일시적인 통신 장애(Timeout, 429, 503 등)가 3회 연속 누적되어 멀쩡한 매장이 비활성화되던 버그를 분석하여, 네트워크 오류 발생 시에는 `miss_count` 누적을 건너뛰도록 분기를 정밀 세분화했습니다.
    *   **Vercel 프록시 우회 (WAF 해결)**: 깃허브 Actions 가상머신 IP가 행안부 방화벽에 로또성으로 걸려 `ETIMEDOUT`이 뜨는 문제를 해결하기 위해 Next.js 백엔드에 `/api/cron/localdata-proxy` API Route를 개설하고 중계 다운로드를 받아 WAF 차단을 100% 회피하도록 아키텍처를 개편했습니다.
    *   **7월 2일 3진 아웃 매장 정밀 복구**: 오늘 새벽 크롤러 에러로 인해 오진단 비활성화 처리되었던 마트 매장 2개(`주식회사 엔마트용인점`, `(주)이마트에브리데이 가운점`)만 정밀 타겟팅하여 수동 DB 활성 상태 복원(`is_active = true, miss_count = 0`)을 안전하게 수행했습니다.

---

## 2. 주요 기술적 결정 사항 (Technical Decisions)

*   **Vercel 프록시 API 중계 구현**:
    *   공공데이터 파일 다운로드 서버(`file.localdata.go.kr`)의 해외 클라우드 IP 차단(WAF)에 대응하기 위해, Next.js 백엔드에 `/api/cron/localdata-proxy` Route를 생성하고 국내 호스팅 중계 연동을 구축했습니다.
*   **3진 아웃 예외 완화**:
    *   네트워크 에러와 진짜 폐업 상태를 분리하기 위해, Playwright 크롤링 에러 문맥 내의 `Timeout`, `429`, `503`, `Network` 등의 단어를 검사하여 일시적인 통신 오류일 경우 `miss_count` 누적을 스킵하도록 예외 필터를 보강했습니다.
*   **GHA 쓰기 권한 명시**:
    *   `github-actions[bot]`이 변경된 커서 파일을 원격 리포지토리에 푸시할 수 있도록 전역 `permissions: contents: write`를 추가했습니다.

---

## 3. 다음 세션 우선 작업 가이드 (Next Steps Guide)

1.  **내일 새벽 배치 구동 결과 확인**:
    *   내일 아침 구동되는 크롤러 및 로컬데이터 프록시 다운로드가 정상 작동하여 어드민 대시보드 및 DB 로그에 `SUCCESS`로 잘 적재되는지 점검합니다.
2.  **구글 플레이스토어 빌드 제출 및 배포 논의**:
    *   TWA 배포를 위한 DUNS 번호 발급 상태를 확인하고, 베타 앱 제출 단계를 추진합니다.

# Session Handoff: Smart Plan Pipeline Optimization (v3.3)

## 1. 현재 상태 요약
이번 세션에서는 Smart Plan 파이프라인의 **1차 선별 로직의 정합성**을 대폭 개선하고, 최근 발생한 **마트 순위 왜곡 문제**를 해결했습니다.

- **SSM 동기화 복구**: `fetch` 에러 및 RPC 명칭 불일치를 해결하여 데이터 파이프라인 정상 가동 확인.
- **마트 선정 로직 개선 (v10.3)**: 
    - 거리 가중치를 60pt로 상향하고, 15km 초과 시 페널티를 부여하여 **근접성** 우선순위 확보.
    - 브랜드 보너스를 등급별(하나로 1위, 대형마트 2위 등)로 세분화.
- **카카오 검증(Phase 2) 강화**: 
    - 상호명이 정확히 일치할 경우 검색 반경을 **10km**까지 허용하는 Fallback 로직 적용 (좌표 드리프트 대응).
    - '할머니어죽' 등 유명 장소가 검색 실패로 누락되던 현상 해결.
- **점수 상한제 폐지**: 기존 100점 캡을 제거하여 고품질 후보군(예: 홍성의료원 128점)의 변별력 확보.

## 2. 기술적 결정 사항
- **Proximity-First Strategy**: 마트 카테고리의 경우 여행 중 '접근성'이 최우선이라는 사용자 피드백에 따라, 브랜드 점수보다 거리 감쇄 로직의 비중을 높임.
- **Organic Growth (Dynamic + Static)**: 마스터 DB의 지역적 데이터 편차를 극복하기 위해, 향후 마트 카테고리도 병원/주유소처럼 **실시간 동적 수집(MT1)** 대상으로 승격시키는 방향을 검토함.

## 3. 다음 작업 가이드
- **카테고리별 1차 선별 로직 고도화**: 마트 외 식당, 명소, 병원 등 전 카테고리의 Scoring Formula를 현장 실효성 중심으로 튜닝.
- **시뮬레이션 반복**: 특정 지역(예산 등)을 기준으로 시뮬레이션을 반복 수행하며 데이터 도출의 적절성 평가.
- **DB 동기화 보강**: 예산/홍성 지역 등 누락된 마스터 데이터를 `sync-master-places.mjs`를 통해 보강하거나 엔진 레벨에서 동적 수집 적용.

## 4. 주의 사항
- **Yesan Data Gap**: 현재 마스터 DB(`master_places`) 내에 예산군 일부 지역의 하나로마트 데이터가 누락되어 있음. (현재는 `smart_plan_facts`의 동적 데이터와 병행 운용 중)
- **API Quota**: 대량 시뮬레이션 시 카카오/공공데이터 API 쿼터 모니터링 필요.

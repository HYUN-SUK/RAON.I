# RAONAI 프로젝트 인수인계 문서 (Handoff)

- **생성 일시**: 2026-05-31
- **세션 완료 상태**: 완료 (빌드 및 컴파일 무결성 완료)

---

## 1. 현재 상태 요약 (이번 세션 완료 사항)
- **NMC 병원 실시간 API 매핑 보완**:
  - `sync-smart-plan/route.ts` API 내에서 실시간 NMC 병원 병상 데이터를 매칭할 때, NMC API 응답에서 주소(`dutyAddr`)가 누락되어 해시 ID(`fid`)가 달라 매핑이 실패하던 문제를 해결했습니다.
  - `liveHospitalsMap` 사전에 `hpid`와 `dutyName`을 추가 Key로 매핑하고, DB의 병원 목록과 대조할 때 `h.raw_data?.hpid` 및 `h.name`을 순차 조회하도록 폴백 로직을 구축하여 완벽히 바인딩에 성공했습니다.
- **일일 지역 로테이션 크론 지오코딩 최적화**:
  - `sync-master-places/route.ts` 크론 API에서 SIDO 루프를 돌 때마다 Supabase 조회를 반복하던 구조를 **루프 진입 전 1회 전체 HOSPITAL 일괄 조회**로 병합하여 성능을 대폭 끌어올렸습니다.
  - 마스터 DB에 이미 위경도를 보유한 병원은 지오코딩 API(카카오) 호출을 **100% 스킵**하게 하였으며, 위경도가 아예 없는 신규 유입 병원에 대해서만 예외적으로 지오코딩을 수행하도록 최적화했습니다.
  - 주소가 누락된 상태로 API가 응답되더라도 기존 DB 내 매치되는 병원(`hpid` 및 `name` 기준)의 올바른 위경도를 찾아내어 지오코딩을 스킵하도록 스킵 사전을 고도화했습니다.
- **검증 무결성**:
  - `npx tsc --noEmit`을 통한 TS 컴파일 무결성 검증 통과 (0 에러).
  - `npm run build`를 통한 production build 무결성 검증 완벽 성공.
  - 예산군 철수네(6/7) 예약 시뮬레이션에서 홍성의료원이 1위로 정상 배치되는 랭킹 점수 정합성 확인.

---

## 2. 기술적 결정 사항
- **실시간 API 주소 누락 대처**: NMC 실시간 병상 정보 API의 `dutyAddr` 미제공 특성에 대응하기 위해, 결정론적 UUID 해시 ID 매칭에서 벗어나 병원의 교유 고유식별자(`hpid`) 및 공식 명칭(`dutyName`/`name`) 기반의 복합 매칭 테이블 구조를 구축했습니다.
- **DB 사전 조회 최적화**: 17개 시도별 NMC 실시간 병원 데이터를 조회할 때 개별 루프마다 DB 조회를 날리는 것보다 전체 HOSPITAL(약 379개) 데이터를 한 번에 가져오는 것이 쿼리 비용과 지오코딩 스킵 필터링 속도 면에서 효율적이라 판단하여 일괄 메모리 매핑 구조로 전환했습니다.

---

## 3. 다음 작업 가이드
- **실제 환경 배치 모니터링**:
  - API 크론 잡(`sync-master-places` 및 `sync-smart-plan`)이 배포 서버에서 스케줄러에 의해 정상 트리거되는지 로그를 모니터링합니다.
  - 카카오 REST API 사용량(Quota) 추이를 확인하여 지오코딩 스킵 로직 적용 전후 대비 트래픽 절감률이 기대치에 도달하는지 검토합니다.

---

## 4. 주의 사항
- **API Key 상태**: `.env.local`의 `PUBLIC_DATA_API_KEY`, `KAKAO_REST_API_KEY` 환경변수가 정상 설정되어 있는지 로컬 개발 시 유의해야 합니다.

# RAON.I Session Handoff - Navigation Stabilization

## 📅 현재 상태 요약 (Current Status)
*   **내비게이션 앱 연동 완료**: 티맵(Tmap), 카카오내비, 카카오맵, 네이버 지도와의 딥링크 연동을 완료하고 안정화했습니다.
*   **GPS 폴백 로직 적용**: 모든 앱 연동 시 수동으로 '출발지'를 지정하지 않고, 내비 앱이 실행될 때의 실시간 GPS(현재 위치)를 자동으로 사용하도록 로직을 개편했습니다.
*   **티맵 안드로이드 호환성 해결**: 최신 티맵 안드로이드 앱에서 `rGoLat` 등의 파라미터를 무시하던 문제를 `goalx`, `goaly` 규격 적용으로 완벽히 해결했습니다.

## 🛠️ 기술적 결정 사항 (Technical Decisions)
*   **출발지 생략 (Omit Origin)**: 사용자가 앱 내에서 위치 정보를 수락하지 않았거나, 프로필의 출발지가 현재 위치와 다를 경우 발생하는 경로 탐색 오류를 방지하기 위해 모든 내비 호출에서 출발지 파라미터를 제거했습니다. (카카오내비의 성공적인 모델을 횡전개)
*   **티맵 규격 분기**: iOS(`rGoX`, `rGoY`)와 안드로이드(`goalx`, `goaly`)의 딥링크 규격이 서로 달라, `navigator.userAgent`를 기반으로 한 분기 로직을 `nav-utils.ts`에 구현했습니다.
*   **정밀도 고정**: 좌표 데이터를 `.toFixed(6)`으로 고정하여 URL 파싱 에러를 예방했습니다.

## 🚀 다음 작업 가이드 (Next Steps)
1.  **실제 기기 최종 확인**: 사용자님께서 `git push` 후 실제 안드로이드/iOS 기기에서 모든 내비 앱이 "현재 위치 -> 목적지"로 즉시 길찾기를 시작하는지 확인이 필요합니다.
2.  **AI 서사 개인화 검증**: 현재 인원 구성(아이/반려견/부모님)과 날씨가 서사에 잘 녹아드는지 다양한 케이스로 테스트가 필요합니다.
3.  **경로 선택 UI 고도화**: 현재는 추천 경로 위주이나, 필요시 `RouteSelector`에서 대안 경로 선택 시 소요 시간 변화 등을 더 직관적으로 보여주는 UI 개선이 가능합니다.

## ⚠️ 주의 사항 (Cautions)
*   **브라우저 녹화 용량**: `browser_recordings` 폴더가 비대해질 수 있으므로, 주기적인 삭제가 권장됩니다. (프로그램 오작동은 아니며, 작업 기록 저장을 위한 용도임)
*   **위치 권한**: 내비 연동 전 브라우저의 위치 권한이 허용되어 있어야 더욱 정확한 폴백이 가능합니다.

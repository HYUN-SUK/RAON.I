# 🤝 Handoff Document: Pipeline Remediation (2026-03-25)

## 1. 현재 상태 요약
오늘 세션에서는 주간 배치 및 D-3 캐싱 파이프라인에서 발견된 데이터 정합성 이슈를 해결하고, 모니터링 편의성을 위해 데이터 소스를 세분화했습니다.

### ✅ 완료된 작업
- **MART RPC 필터링 수정**: PostGIS RPC(`get_master_places_in_radius`)의 카테고리 필터링 파라미터 불일치 문제 해결 및 수동 마이그레이션 완료.
- **주유소 주소 보강**: 오피넷 API 주소 누락 시 WGS84 좌표 기반 카카오 역지오코딩 폴백 로직 추가.
- **마트 소스 세분화**: `LOCALDATA_MART`를 `LARGE`, `SSM`, `SUPER`로 분리하여 관리자 모니터링 정교화.
- **SSOT 매뉴얼 업데이트**: 수정보완된 로직을 `smart_camping_plan_manual.md`(v10.1)에 반영.

## 2. 기술적 결정 사항
- **RPC 파라미터 표준화**: 기존 `target_category`를 `p_category`로 표준화하여 시스템 전반의 명명 규칙 통일성 확보.
- **역지오코딩 폴백**: API 원본 데이터(`VAN_ADR`)가 없을 경우에만 카카오 API를 호출하여 비용 효율성과 데이터 정확도의 균형을 맞춤.

## 3. 다음 작업 가이드
- **시뮬레이션 재검증**: 수정된 로직으로 주간 배치와 D-3 캐싱 시뮬레이션을 다시 돌려 마트 및 주유소 데이터가 의도대로 선별되는지 확인.
- **1차 후보군 실효성 분석**: 선별된 후보군이 실제 사용자에게 매력적인지(브랜드 비중, 거리 적절성 등) 심층 분석.
- **실효성 낮은 후보군 보완**: 분석 결과에 따라 가중치(`reliability.ts`) 또는 필터링 쿼리를 미세 조정.

## 4. 주의 사항
- **브라우저 캐시**: 주익스 로직 변경 후 프론트엔드에서 이전 캐시 데이터가 보일 수 있으므로 테스트 시 강력 새로고침 필요.
- **Supabase 마이그레이션**: 로컬의 `supabase/migrations` 파일과 실제 DB의 RPC 정의가 일치하는지 항상 확인.

# Session Handoff: v11.9.25 Smart Plan Infrastructure

## 📝 현재 상태 요약
- **AI 엔진 업그레이드**: Gemini 1.5 Flash에서 **Gemini 2.5 Flash-Lite**로 모델 전환 및 프롬프트 최적화 완료.
- **5단계 감성 타임라인**: 여정을 5개 단계(출발-경유-준비-힐링-귀가)로 세분화하고 각 단계마다 AI가 작성한 연결 문구가 노출되도록 UI 개편.
- **API 안정성 확보**: 보안 문제(환경 변수) 해결을 위해 `/api/smart-plan` 엔드포인트를 신설하여 모든 AI 로직을 서버 사이드로 이전.
- **데이터 복구**: 예약 데이터가 매칭되지 않던 날짜 로직과 ID 조회 로직을 수정하여 Track A 데이터가 정상적으로 불러와짐.

## ⚙️ 기술적 결정 사항
- **Server-Side Rendering**: 클라이언트 컴포넌트에서 직접 AI를 호출하던 방식을 지양하고 API Route를 경유하도록 함. 이는 `SUPABASE_SERVICE_ROLE_KEY` 등 민감한 정보 보호 및 안정적인 DB 조회를 위함.
- **Modular Prompting**: 100개가 넘는 모든 후보지(Primary + Alternatives)에 대해 한꺼번에 한 줄 소개를 생성하도록 프롬프트를 설계하여, 사용자가 장소를 교체(Swap)해도 추가 로딩 없이 즉시 AI 추천 문구를 볼 수 있게 함.
- **Stage 5 Fallback**: 새로운 검색 없이 Track B의 대안 리스트(2순위)를 활용하여 '귀갓길 추천'을 생성함으로써 성능 효율성 극대화.

## 🚀 다음 작업 가이드 (Phase 11)
- **UI 렌더링 수정**: 현재 장소 카드에서 **인증 마크(백년가게, 8경 등)**와 **AI 한 줄 설명**이 간헐적으로 노출되지 않는 문제 해결 필요. (`SmartPlanProposal.tsx`의 카드 렌더링 로직 점검)
- **내비게이션 연동**: 각 장소 카드에 **카카오맵, Tmap, 카카오내비**로 바로 연결되는 딥링크 버튼 추가.
- **개인화 품질 점검**: AI가 생성한 `oneLiners`가 사용자의 페르소나(예: 아이 동반 여부)를 충분히 반영하고 있는지 실제 응답 데이터 검토.
- **인터랙션 보완**: 장소 교체 시 타임라인의 흐름이 자연스럽게 유지되는지 UX 테스트.

## ⚠️ 주의 사항
- **API Quota**: Gemini 2.5 Flash-Lite 무료 쿼터(15 RPM) 내에서 테스트 중이므로, 반복적인 빠른 호출 시 에러가 발생할 수 있음.
- **Local Cache**: `nearby_cache` 테이블 관련 RLS 에러가 로그에 찍히고 있음. 기능에는 지장이 없으나 향후 권한 수정 필요.

# RAON.I 프로젝트 인수인계 문서 (Handoff Document)

**작성 일시**: 2026-09-23T11:20:00+09:00  
**기준 브랜치**: `main`  
**빌드 상태**: Next.js 16.1.1 Production Build (103/103 전체 라우트 100% 정상 통과, TypeScript 0에러)  
**실서버 배포**: Vercel 프로덕션 배포 완료 (`https://raon-ai.com`), 카카오 간편로그인 실기기 검증 완료  

---

## 1. 현재 상태 요약 (Current State & Completed Work)

### 🟢 마일스톤 9.64: 정밀 여행계획(SmartPlanProposal) 2단계 계층형 뒤로가기(Popstate) 가드 구축 및 페이지 이탈 완치 (2026-09-23)

1. **대체리스트 지도로 보기 Level 2 히스토리 스택 적재 (`SmartPlanProposal.tsx`)**:
   - 대체 장소 리스트(depth 1)에서 [🗺️ 지도로 보기] 진입 시 `window.history.pushState({ raonProposalSubsheet: 2 })`로 depth 2를 등록하여 모바일 뒤로가기 방어선 2중화.
2. **지도로 보기에서 2단 뒤로가기 안전 복원**:
   - 1차 뒤로가기: 지도만 닫힘 ➔ 대체 장소 리스트 바텀시트 복원 (depth: 2 ➔ 1).
   - 2차 뒤로가기: 대체 장소 리스트 닫힘 ➔ 스마트플랜 본문 복귀 (depth: 1 ➔ 0).
   - 지도 확인 후 뒤로가기를 연속 클릭했을 때 일정 상세 페이지(`/myspace/schedule/[id]`) 밖으로 튕겨 나가던 결함 100% 원천 박멸.
3. **`isProgrammaticBackRef` & `go(-depth)` 안전 회수 장착**:
   - UI 버튼(`[📋 리스트로 보기]`, `[X]`, `[✔ 이 장소로 선택]`) 터치 시 브라우저 popstate 중복 트리거 차단 및 찌꺼기 가상 히스토리 일괄 소멸. 언마운트 시 브라우저 세션 클린업.
4. **빌드 검증**:
   - `npx tsc --noEmit` 에러 0건 통과 및 Next.js 16.1.1 Production Build 103/103 전체 라우트 100% 정상 통과.

### 🟢 마일스톤 9.63: 즉시 여행계획 모바일 내비게이션 3대 결함 완치, 모달 종료 시 세션 상태 완전 리셋 및 카카오 간편로그인 안정화 완결 (2026-09-22)

1. **즉시 여행계획 3대 내비게이션 결함 보완 (`InstantPlanModal.tsx`, `SmartPlanProposal.tsx`, `SmartPlanMapViewModal.tsx`)**:
   - **GPS 갱신 시 플랜 보존**: 모바일 GPS 신호 갱신이나 위치 재측정 시 이미 생성된 4단계 여행 플랜(`planData`)이 날아가거나 초기화되지 않고 안전하게 보존되도록 가드 처리.
   - **모바일 3단계 계층형 히스토리 가드 구축**:
     - 스마트폰 하단 '뒤로가기(Back Button)' 제스처/버튼 터치 시 앱이 꺼지거나 화면이 엉뚱하게 튕기는 현상 완벽 방어.
     - **3단계 계층형 히스토리 스택**: `지도 팝업` ➔ [뒤로가기] ➔ `대체 장소 리스트 바텀시트` ➔ [뒤로가기] ➔ `즉시 여행계획 결과 모달` ➔ [뒤로가기] ➔ `홈 화면 잔류(모달만 닫힘)`.
   - **대체리스트 '변경' 터치 시 즉시 닫힘 생명주기 오류 해결 (`20de41b`)**:
     - 대체리스트에서 장소 변경 버튼 터치 시 `popstate` 리스너와 시트 언마운트 타이밍 경합으로 시트가 비정상 종료되던 cleanup 생명주기 오류를 완벽 치유.

2. **모달 종료(`!isOpen`) 및 홈 복귀 시 이전 여행계획 소멸 및 세션 상태 완전 리셋 (`9345435`, `fd762b6`)**:
   - 모달 우측 상단 닫기(X) 또는 뒤로가기로 모달을 빠져나갈 때, 이전 생성 플랜 데이터(`planData`), 대체리스트 상태, 임시 입력값이 완전히 초기화.
   - 다시 모달을 열었을 때 이전 잔류 데이터 없이 깨끗한 첫 시작 화면(위치 확인 및 탐색 모드)으로 동작하도록 리셋 파이프라인 구축.

3. **카카오 간편로그인 세션 복구 및 안정화 검증**:
   - **오진 및 오버엔지니어링 원천 차단**: 소셜 로그인 시작 전 `supabase.auth.signOut()` 호출로 인한 모바일 브라우저 대기(Hang) 및 1회용 OAuth 인가 코드의 브릿지 재사용(400 Bad Request 튕김 루프)을 제거하고, 완벽히 검증된 안정 기준점(`fd762b6`)으로 롤백 및 동기화.
   - **실서버 및 스마트폰 실기기 검증 완료**: Vercel 프로덕션 배포 후 사용자가 직접 스마트폰에서 카카오 간편로그인 시도하여 정상 로그인 동작 확인 ("지금은 된다!!").

4. **과거 핵심 마일스톤 100% 온전히 보존**:
   - **마일스톤 9.60~9.62**: 관리자 예약정보 전체 항목 수정(3중 연동·회계 차액 전이·AI 스마트플랜 보존), 본 예약 유지 + '일부 환불(차액)' 독립 분리, 추가 입금(차액) 분리, 결제목록 스마트 3계층 정렬(제자리 유지) 기능 100% 정상 작동 보존.
   - **스마트플랜 Track B(오고 가는 길) 거리 미표시 원칙** 100% 보존.

---

## 2. 기술적 결정 사항 (Architectural & Technical Decisions)

### 1) 3단계 계층형 히스토리 스택 (Hierarchical History Stack)
- **배경**: 모바일 웹 환경에서 유저는 카카오 지도 팝업이나 대체 장소 리스트 바텀시트를 연 뒤 브라우저 하단 '뒤로가기'를 눌러 이전 레이어로 돌아가기를 기대함. 그러나 단일 히스토리 구조에서는 브라우저 자체가 이전 페이지(외부 사이트 등)로 튕겨 나가는 결함이 발생.
- **해결**:
  - 각 서브 레이어(지도 모달, 대체리스트 바텀시트, 즉시플랜 메인 모달) 오픈 시 각각 고유의 `history.pushState`를 발행.
  - `popstate` 이벤트 핸들러를 통해 최상위 레이어부터 역순(`지도 -> 대체리스트 -> 메인모달 -> 홈`)으로 순차 소멸시키며, 마지막 모달 닫힘 시 홈 화면에 온전히 머물도록 제어.

### 2) 모달 종료 시 세션 상태 완전 리셋 (`useEffect` Cleanup)
- **배경**: 즉시 여행계획 모달을 닫았다가 다시 열었을 때, 직전에 보았던 여행 플랜 결과가 메모리에 남아있어 새로운 조건으로 탐색하려는 유저에게 혼선을 초래함.
- **해결**:
  - `isOpen === false`가 되는 순간 `setPlanData(null)`, `setAlternativeSheet(null)` 등 내부 상태를 즉시 클린업하여 재진입 시 항상 청정한 상태에서 출발하도록 보장.

### 3) 소셜 로그인(OAuth) 생명주기 원칙 확립
- **네트워크 signOut 금지**: 소셜 로그인 버튼 터치 핸들러 내에 원격 서버 통신을 유발하는 `signOut()`을 삽입할 경우, 모바일 브라우저 환경에서 네트워크 지연/락으로 인해 카카오 로그인 창으로의 리다이렉트가 블로킹됨. 세션 정리는 로그아웃 시점에만 완결해야 함.
- **OAuth 인가 코드 1회성(Single-use) 준수**: RFC 6749 표준에 따라 OAuth authorization code는 서버(`route.ts`)에서 단 1회 교환(`exchangeCodeForSession`)되는 즉시 소멸됨. 서버 실패 시 클라이언트로 코드를 전달해 재교환하려는 시도는 무조건 400 Bad Request를 유발하므로 금지.

---

## 3. 다음 작업 가이드 (Next Steps for Next Session)

다음 세션에서 우선적으로 진행할 수 있는 추천 작업 목록:

1. **즉시 여행계획 실제 사용자 체감 사용성 모니터링**:
   - 모바일 현장(스마트폰 기기별: iOS Safari, Android Chrome, 카카오톡 인앱 브라우저 등)에서 GPS 수신 및 4단계 코스 생성 속도 실측 관제.
2. **관리자 결제/예약 목록 운영 현황 확인**:
   - 마일스톤 9.60~9.62를 통해 개편된 예약정보 수정 및 차액(일부환불/추가입금) 독립 행 처리 동작이 실운영 환경에서 원활히 유지되는지 점검.
3. **스마트플랜 캐싱 및 배치 모니터링**:
   - 일일 지역 로테이션 및 스마트플랜 캐싱 GitHub Actions 배치 정상 실행 여부 관제.

---

## 4. 주의 사항 및 잠재적 위험 요소 (Warnings & Tech Debt)

> [!CAUTION]
> **소셜 로그인 로직 임의 변경 금지**
> 현재 `src/app/auth/callback/route.ts` 및 `SocialLoginButtons.tsx`는 카카오 OAuth 표준 스펙에 따라 검증된 안정 상태입니다. 로그인 시작부에 비동기 네트워크 통신(`signOut`)을 추가하거나 인가 코드를 브릿지로 재전달하는 시도는 모바일 튕김을 유발하므로 절대 금지합니다.

> [!IMPORTANT]
> **스마트플랜 Track B 거리 미표시 원칙 절대 유지**
> 정밀 스마트플랜 Track B(오고 가는 길)에서는 장소 간 이동 거리를 표기하지 않는 비즈니스 정책이 적용되어 있습니다. UI 수정 시 해당 영역에 거리가 강제로 노출되지 않도록 기존 가드를 유지해야 합니다.

> [!NOTE]
> **모바일 뒤로가기 가드 유지**
> 모달 및 바텀시트에 적용된 `history.pushState` 및 `popstate` 핸들러는 모바일 뒤로가기 시 홈 잔류를 보장하는 필수 안전장치이므로, 내비게이션 관련 컴포넌트 수정 시 생명주기 정합성을 확인해야 합니다.

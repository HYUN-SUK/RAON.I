# 📑 프로젝트 인수인계 문서 (Handoff Document)

**작성 일시**: 2026-08-10
**프로젝트명**: RAON.I (라온아이 - 스마트 여행 수첩)
**작성자**: Antigravity AI Assistant

---

## 1. 현재 상태 요약 (Current Accomplishments)

이번 세션에서는 일일 자동화 안정화, 안드로이드 브릿지 구축 및 핵심 골든 타임 권한 동의 유도 팝업 구현을 완수했습니다.

*   **[완료] 일일 로테이션 갱신 중복 트리거 해제**:
    *   `.github/workflows/daily-region-sync.yml`: 깃허브 자체 `schedule` 크론 구문을 주석 처리(비활성화)하여 external cron dispatch 전용 **1일 1회 단독 실행**으로 지연과 중복을 차단했습니다.
*   **[완료] 명소(TourAPI) 오류 로깅 상세화**:
    *   `scripts/daily-region-sync.mjs`: 관광 명소 수집 실패 시 통신 예외 구문(예: `💥 ERROR (${e.message.slice(0, 30)})`)을 `stat.note` 컬럼에 남기도록 보완하여 트래픽 초과나 타임아웃을 즉시 디버깅할 수 있게 패치했습니다.
*   **[완료] 안드로이드 푸시 브릿지 연동**:
    *   `src/hooks/usePushNotification.ts`: `window.onReceiveAndroidToken` 전역 브릿지 리스너를 연동하여 네이티브 패키징 시 토큰 자동 적재 기반을 구축했습니다.
*   **[완료] 골든 타임 기반 위치 및 알림 권한 동의 유도 팝업 연동**:
    1.  **예약 완료 화면 (`complete/page.tsx`)**: 예약 완료 시 기기 알림 미동의 유저에게 `PushPermissionPrompt` 모달 연동.
    2.  **빈자리 알림 신청 시 (`WaitlistButton.tsx`)**: 알림 신청 클릭 시 알림 권한 가드 검사 및 동의 모달 연동.
    3.  **내 주변 정보 조회 시 (`play/page.tsx`, `recipe/page.tsx`)**: 마트/놀거리/날씨 조회 시 위치 미동의 유저에게 `LocationPermissionPrompt` 연동 및 거부 시 기본 좌표 우회로 작동.
    4.  **중복 팝업 차단**: 이미 허용(`granted`)을 마친 사용자는 0.01초 스킵 조치.
*   **[완료] 스마트플랜 소스코드 원본 보존**:
    *   사용자의 요청에 따라 스마트플랜(`SmartPlanProposal.tsx`) 소스코드와 유효성 검사는 일절 수정하지 않고 100% 원본 유지했습니다.
*   **[완료] 마스터 로드맵 최후반 Phase 13.0 등록**:
    *   `RAON_MASTER_ROADMAP_v3.md`: 로드맵 최후반 단계로 `Capacitor.js 기반 하이브리드 네이티브 앱 전환 및 정식 스토어 등록`을 반영 완료했습니다.

---

## 2. 주요 기술적 결정 사항 (Technical Decisions)

1.  **스마트플랜 코드 완전 보존**:
    *   스마트플랜의 출발지 입력 검사 및 로직을 유지하고, 권한 유도 팝업 패치는 실제 권한이 요구되는 터치포인트(예약완료, 빈자리신청, LBS조회) 3곳에만 정밀하게 적용했습니다.
2.  **권한 팝업 피로도 방지 가드**:
    *   `Notification.permission === 'granted'` 및 브라우저 Geolocation status 조회를 1차 판단 가드로 배치하여, 이미 동의 완료한 유저에게는 동의 모달이 단 1회도 노출되지 않도록 설계했습니다.
3.  **거부(Dismiss) 시 안전 대피로**:
    *   위치 권한을 끝까지 거부하거나 브라우저 제약이 있더라도 앱이 먹통이 되지 않고, 기본 캠핑장 좌표(아산/서울) 폴백을 가동하도록 우회로를 보장했습니다.

---

## 3. 다음 작업 가이드 (Next Steps)

1.  **권한 동의 유도 팝업 모니터링**:
    *   배포된 위치/알림 동의 팝업이 실제 사용자 환경에서 유저 수락률 및 토큰 적재율을 올리는지 확인.
2.  **카카오톡 알림톡 연동 사전 준비 (선택사항)**:
    *   브라우저 권한 차단이나 세션 유실 없이 100% 수신율을 보장하기 위한 카카오 알림톡(비즈니스 채널 개설 및 템플릿 신청) 연동 준비.
3.  **마스터 로드맵 차기 기능 개발**:
    *   `RAON_MASTER_ROADMAP_v3.md` 상의 다음 순서 기능 고도화 진행.

---

## 4. 주의 사항 (Caveats & Known Issues)

*   **스마트플랜 출발지 입력**: 스마트플랜 생성 시 기기 위치 권한이 꺼져 있고 프로필 주소도 없으면 출발지 미입력으로 인한 안내 토스트가 뜰 수 있으며, 이는 의도된 원본 로직입니다.
*   **PWA Builder 배포 상태**: PWA Builder 웹뷰 환경에서는 네이티브 자바 브릿지가 없으므로 `window.onReceiveAndroidToken`은 대기 상태이며, 웹 푸시 FCM 및 서비스워커 기반으로 정상 발송됩니다.

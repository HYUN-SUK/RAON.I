# 📋 프로젝트 인수인계 및 Gemini 1줄 적재 시행착오 현황 보고서

본 문서는 현재까지 라온아이(RAON.I) 프로젝트의 **Gemini 1줄 설명 사전 적재 작업** 도중 겪은 시행착오 현황과 요금 정산 실사 데이터, 그리고 다음 세션에서 이어나갈 비용 점검 및 작업 방향에 대해 인수인계하기 위해 작성되었습니다.

---

## 1. 현재 상태 요약 (Current Status)
* **식당(RESTAURANT) 카테고리**: **100% 전수 적재 마감 완료**
  * 총 `80,468`건 중 `51,436`건 Gemini 요약 생성 성공,나머지 `29,032`건 0원 로컬 Fallback 우회 적용.
  * 카페(`ROUTE_CAFE`)는 실제 DB에 존재하지 않아 식당 카테고리에 완벽히 통합 처리 완료.
* **마트(MART) 카테고리**: **100% 전수 적재 마감 완료 및 임시 덮어쓰기 보류**
  * 총 `11,309`건 중 `1,766`건 최신 고품질 덮어쓰기 완료,나머지 구형 1줄설명(`7,172`건) 및 0원 Fallback(`2,371`건) 상태로 추가 결제 제어를 위해 루프 안전 종료.
* **비용 점검 테스트 완료**:
  * 7월 1일 자로 **동시성 1개(Concurrency 1),딜레이 1초(1000ms)** 조건 하에 딱 **10건**의 마트만 한정 호출하는 정밀 과금 테스트를 완료하고 프로세스 안전 종료.

---

## 2. 💸 Gemini 1줄 적재 시행착오 및 과금 폭증 실사 현황 (핵심)

### **① 식당 5만 건 적재 및 누적 요금의 진실**
* **의문**: 1건당 0.1원~0.2원 수준이라면 식당 5만 건 적재 시 약 5,000원~6,000원이 나왔어야 하는데,사용자 선불 충전금 25,000원이 완전히 바닥나 마이너스 1,000원(총 26,000원 차감)이 깎인 이유.
* **정밀 분석 결과**:
  * DB 타임스탬프 실사 결과,식당 51,436건 중 **무려 `41,912` 건 (약 80%)은 6월 17일~19일 사이 과거 개발 단계에서 이미 적재 완료**되어 있었습니다.
  * 어제/오늘 우리가 새로 호출한 신규 식당 건수는 **`9,531` 건**에 불과합니다.
  * 6월 17일부터 오늘(7월 2일)까지 보름간 5만여 건의 전체 요약 호출에 대해 최종 확정 청구된 구글 결제액은 정확하게 **`36,600원`**입니다.
  * 이를 역산하면 **1건당 평균 사용 단가는 정확하게 `0.71원`**이 매겨졌음을 의미합니다! (1건당 0.7원~1원 수준으로,당초 단가 계산과 정확히 수렴합니다.)

### **② 10건 테스트 시 51원(건당 5.1원)이 찍힌 구글 정산의 맹점**
* **의문**: 10건의 마트만 1초 간격으로 돌렸는데 크레딧 잔액판에서 왜 56원이 깎이고 최종 하루 지출 영수증(SKU)에 **51원**이 청구되었는가?
* **정밀 분석 결과**:
  * 입력이 짧은 쿼리(`short input text`)에 대한 출력 토큰 SKU 단가가 높게 할증 적용되었습니다. (입력 1원,출력 50원 청구).
  * 구글 AI Studio 결제망의 원화(`₩`) 정산 단위 해상도 한계로 인해,하루 치 발생한 API 요금이 100원 미만으로 극도로 작을 경우 **최소 청구 금액인 51원으로 원장 올림(Rounding up)** 처리를 진행하기 때문입니다.
  * 반면,수만 건을 한꺼번에 몰아서 대량 적재(Bulk Batch)를 돌릴 때는 이 올림 오차가 거의 다 상쇄되고 원래 단가인 **1건당 0.71원 요율로 정확하게 수렴**하게 됩니다.

---

## 3. 기술적 결정 사항 (Technical Decisions)
* **Supabase 카테고리 200건 슬라이싱 튜닝**:
  * DB 전체 17만 건 스캔 시 발생하는 10초 타임아웃 문제를 해결하기 위해,카테고리 필터링(`eq('category', ...)`) 쿼리 시 `limit` 사이즈를 **`200`**으로 대폭 제한하여 DB 타임아웃을 차단함과 동시에 마트 데이터 수집 효율을 2.5배 이상 고속화시켰습니다.
* **임시 테스트 가드 롤백**:
  * 다음 세션에서 전체 벌크 적재를 재개할 때 오작동이 없도록,10건 제한 강제 종료 브레이크 가드는 `gemini-enrich-description.mjs` 소스 상에서 정상적으로 원복 롤백해 두었습니다.

---

## 4. 다음 작업 가이드 및 우선순위 (Next Steps)
1. **결제 잔액/정산 모니터링 및 추가 비용 점검**:
   * 선불 크레딧 잔액(`₩9,790`)에서 어제/오늘 가승인 잠금(Authorization Hold)되었던 예치금이 완전히 취소 및 복구되어 잔액 원장이 다시 상향 조정되는지 며칠간 금융망 원장을 추가 관찰합니다.
2. **비용 효율적인 우회로(Key Rotation) 대안 수립**:
   * 유료 PRO 등급 계정 대신,비용이 0원인 **무료 등급 API Key 3~4개를 발급받아 키 로테이션(Key Rotation) 방식으로 호출**하여 1원도 과금되지 않는 0원 무과금 적재 방식을 다음 세션 기획 단계에서 설계 및 적용합니다.
3. **3단계 명소/공공 카테고리(SPOT) 사전 적재 기동**:
   * 비용 검증 및 키 로테이션 대안이 수립되면,가장 대용량인 관광명소(`SPOT`) 카테고리 1줄 적재를 재개합니다.

---

## 5. 주의 사항 (Cautions)
* **임의 수동 킬(Kill) 주의**: API 호출이 완료된 직후 DB 쓰기(Upsert)가 수행되기 직전에 프로세스를 `kill` 하면 API 요금만 과금되고 DB에 적재되지 않고 데이터가 유실되므로,반드시 배치가 완전히 끝난 뒤 정지해야 합니다.

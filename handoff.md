# [Handoff] 명성 데이터 보강 완료 및 하이브리드 엔진 통합 준비 (v14.1)

대한민국 전국 229개 시군구의 명칭 마스터 보강(Stage 1)을 성공적으로 마쳤습니다. 다음 세션에서는 이 데이터를 실제 서비스 엔진에 이식하는 Stage 2를 진행합니다.

## 1. 현재 상태 요약
- **전국 명성 데이터 확보 (100%)**: 전국 기초지자체 전수 조사(229개)를 통해 총 1,966개의 고밀도 명소 데이터를 `regional_8_sceneries_FULL.md`에 구축 완료.
- **통합 계획 수립 (v14.1)**: 명성(60%) + 인기(40%) 결합 알고리즘 및 '명성 보장제(is_protected)' 방어 로직 설계 완료.
- **시스템 검증**: 전체 코드 빌드(`npm run build`) 성공 및 현재 상태 깃 커밋 완료.

## 2. 주요 기술적 결정 사항
- **6:4 하이브리드 스코어링**: 티어 1(100점), 티어 2(50점) 합산을 60% 비중으로 두고, TMAP/KT/KTO(공공데이터) 인기도를 40%로 결산함.
- **명성 보호막 (Defense Logic)**: 명성 데이터에 `is_protected: true` 속성을 부여하여 지역 동기화 로테이션 시 API 응답 누락으로 인한 자동 비활성화를 원천 차단.
- **인기도 지표 확장**: TMAP/KT 외에 '한국관광공사_기초지자체 중심 관광지 정보' API를 추가하여 공신력 있는 인기도 축 확보.

## 3. 다음 세션 전수 가이드
- **우선 순위 1**: `sync-prestige-data.mjs`를 생성하여 티어 1, 2 데이터를 DB(`prestige_landmarks`)로 이관하고 마스터 DB에 없는 항목 강제 삽입.
- **우선 순위 2**: `caching-smart-plan.mjs`의 스코어링 인터페이스를 하이브리드 수식으로 교체하고 실효성 검증(Audit Report).
- **우선 순위 3**: 관리자 자동화 로그 화면에 신규 API 소통 현황 UI 이식.

## 4. 주의 사항
- **주소 정규화**: 마스터 DB 합병 시 `getNormalizedAddr`를 사용하여 중복 등록을 방지할 것.
- **티어 1 처리**: "5대 고궁" 등 묶여있는 항목은 DB 적재 시 개별 명소로 분리하여 등록 필요.

---
**다음 세션 키워드**: `Stage 2 데이터 통합 실행`, `하이브리드 엔진 검증`

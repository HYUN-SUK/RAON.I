# Handoff: Smart Plan Hospital & Data Pipeline Optimization

## 1. 현재 상태 요약 (Current Status)
- **병원 데이터 복구 완료**: NMC API에서 좌표 없이 내려오던 대학병원 데이터를 카카오 지오코딩으로 실시간 복구하여 DB 유실을 차단했습니다.
- **주소 정규화 버그 해결**: '강원특별자치도' 등 특수 행정구역 명칭 처리 시 시/군/구가 잘못 추출되던 정규표현식 오류를 수정했습니다.
- **스코어링 밸런스 최적화**: NMC 응급의료센터(150점), 일반 종합병원(100점), 야간 응급 의원(90점) 체계를 구축하여 안전과 근접성을 모두 고려한 순위를 구현했습니다.
- **데이터 무결성 확보**: 춘천(강원도립화목원) 예약건에 대해 최신 로직으로 캐싱을 재실행하여 한림대/강원대병원이 상위에 노출됨을 확인했습니다.

## 2. 기술적 결정 사항 (Technical Decisions)
- **NMC 우선순위**: NMC API는 실시간 병상 정보를 제공하므로 신뢰도가 가장 높다고 판단, 기본 점수를 150점으로 설정하여 최상단 노출을 보장했습니다.
- **주소 추출 로직 (v11.9.65)**: 정규표현식 선택지 순서를 긴 명칭부터(특별자치도 -> 도 -> 시) 배치하여 최장 일치 매칭이 되도록 개선했습니다.
- **격리된 캐싱 파이프라인**: 동일 사용자의 다중 예약 시 데이터 혼선을 막기 위해 클러스터별로 메모리 맵을 독립적으로 관리하도록 구조를 고착화했습니다.

## 3. 다음 작업 가이드 (Next Steps)
- **AI 히어로 문구 점검**: AI가 생성하는 일정 요약 및 히어로 문구가 동반 가족 정보(고령자 등)를 정확히 반영하는지 프롬프트 고도화 및 결과물 감사(Audit).
- **축제 스코어링 정밀화**: `SPOT`과 `FESTIVAL` 간의 점수 배점 차이를 재점검하고, 불필요한 `readcount` 기준을 제거하여 최신 데이터 기반으로 최적화.

## 4. 주의 사항 (Caveats)
- **Git 상태**: 현재 빌드 및 로컬 커밋이 완료된 상태입니다. 세션 시작 시 반드시 `git push`를 먼저 진행하시기 바랍니다.
- **API 쿼터**: NMC 복구 로직에서 카카오 지오코딩 API를 추가로 사용하므로, 대량 캐싱 시 카카오 API 쿼터 사용량을 모니터링할 필요가 있습니다.

# Session Handoff

## 📌 현재 상태 요약
이번 세션에서는 스마트 캠핑 플랜의 핵심인 **Hybrid Enrichment Pipeline (Phase 11 & Phase 12)** 구조를 완벽하게 통합 및 고도화했습니다.
- **Phase 11**: 10만 건 이상의 전국 단위 원시 데이터를 10ms 이하로 검색하는 `master_places` PostGIS 쿼리 시스템을 구축 완료했습니다. 비/맑음 날씨에 따른 **1차 후보군 가중치 선별 로직(`route.ts`)**을 도입하여 단순 반경 검색을 넘은 맞춤형 선별 체계를 구비했습니다.
- **Phase 12**: 카카오맵 스크래퍼(`scraper.ts`)를 직접 제작하여 봇 차단을 우회하고, 실시간 별점과 리뷰 수를 추출하는 파이프라인을 크론잡 로직에 이식 완료했습니다. 
- **SSOT 매뉴얼 일원화**: 개발된 시스템의 스펙(PostGIS 공간쿼리, 카카오 크롤링 안티봇 헤더 우회, 9 카테고리 등)과 과거 발생한 기상청/공공API ETL 장애 회피 전략까지 모두 `docs/smart_camping_plan_manual.md`에 단일 진실 공급원(SSOT)으로 기록 복구 및 최신화했습니다.
- 빌드 테스트 통과 후 깃 커밋까지 완료했습니다.

## 🛠 기술적 결정 사항
1. **API 호출 대신 하이브리드 캐싱 선택 (Phase 11)**: 수많은 공공 API의 잦은 응답 실패(XML 찌꺼기, 403 에러 등)를 회피하기 위해 월요일 새벽 1차 풀-동기화를 거친 뒤 실시간 엔진에서는 Supabase 내부망(RPC)만 타도록 분리했습니다. 결과적으로 성능과 안정성을 모두 잡았습니다.
2. **카카오맵 JSON 직접 호출 (Phase 12)**: 카카오 로컬 API로는 별점/리뷰 수를 얻을 수 없고, HTML 크롤링은 CSR과 봇 차단에 막혔기 때문에, 네트워크 탭을 리버스 엔지니어링하여 발견한 비공개 JSON 엔드포인트(`place-api.map.kakao.com/...`)에 `Referer`와 `User-Agent` 헤더를 씌워 뚫어냈습니다.
3. **카페 전용 데이터 소스 확보**: 식당 API에만 의존하던 기존의 `ROUTE_CAFE` 추천 품질 문제를 해결하기 위해 한국관광공사의 테마(`contentTypeId=39`) 필터링을 도입하여 양질의 카페 데이터를 확보했습니다.

## 🔜 다음 작업 가이드 (Next Session Focus)
- [ ] **1차 선별 로직(Phase 11) 고도화 완성**: 오늘 추가한 비 오는 날 대비 로직에 이어, '맑은 날' 가중치 로직 적용 및 유저 페르소나(아이 동반 등)의 복합적 가중치를 추가.
- [ ] **카페 데이터품질 강화**: 새롭게 확보한 `TOUR_CAFE` 소스와 카카오 검색 데이터간 정교한 매칭 로직 개발.
- [ ] **AI 서사(Step 8) 디테일 개선**: 이번 세션에서 모은 진짜 스크래핑 별점 데이터를 AI가 더욱 자신감 있고 자연스럽게 문장에 인용하도록 프롬프트 디테일 고도화.

## ⚠️ 주의 사항 / 알려진 버그
- `smartPlan.ts`에서 디버깅을 위해 써두었던 주요 `console.log`는 지웠으나, `sync-master-places/route.ts`에 있는 `[Master Sync Cron]...` 로그들은 배치 모니터링을 위해 의도적으로 남겨두었습니다. 
- 카카오맵 비공식 엔드포인트를 사용중이므로 카카오 측 서버 정책 변경 시 스크래퍼가 0점을 반환할 가능성이 항상 존재합니다. 현재 에러 방어(Catch)가 되어 있어 크래시는 없지만, 0점이 고정으로 나오기 시작하면 스크래퍼 URL과 헤더를 다시 점검해야 합니다.

"# 전남광주시(시도 통합) 출범에 따른 영향도 분석 및 보완 계획\n\n7월부로 시행되는 광주광역시와 전라남도의 통합 행정구역인 **'전남광주시'** 출범과 관련하여, 일일 지역 로테이션 갱신 엔진, 마스터 DB 적재, 공공 API 연동, 그리고 앱 서비스 호출 전반에 걸친 영향도를 점검하고 이에 대한 구체적인 보완 계획을 보고합니다.\n\n> [!WARNING]\n> **핵심 위험 요인: 공공 API 업데이트 지연 및 UUID 중복 적재**\n> 행정구역이 개편되더라도 각 정부 부처(농식품부, 관광공사, 의료원, 행안부 등)의 공공 API 데이터가 실제 개편안을 즉각 반영하는 데는 수개월 이상의 지연이 발생할 수 있습니다. \n> 이 과도기에 적절한 방어 로직(Failsafe)이 없으면 **(1) 기존 데이터의 대규모 유실, (2) UUID 불일치로 인한 데이터 중복 적재, (3) 일일 로테이션 수집 장애**가 발생할 수 있습니다.\n\n---\n\n## 1. 세부 영향도 분석 (Impact Analysis)\n\n### A. UUID Hashing 일관성 파손 (가장 치명적)\n- **현황**: `master_places` 테이블의 고유 ID(UUIDv5)는 `getNormalizedAddr(addr)` 함수를 통해 정규화된 주소 문자열을 해싱하여 생성됩니다.\n- **문제점**: \n  - 기존 주소: `\"광주광역시 북구 ...\"` -> UUID `A` 생성\n  - 신규 주소: `\"전남광주시 북구 ...\"` -> UUID `B` 생성\n  - 7월 이후 API나 크롤링을 통해 '전남광주시' 주소로 데이터가 유입되면 UUID `B`가 생성되어 **중복 적재**됩니다.\n  - 동시에, 기존에 적재되어 있던 UUID `A` 데이터는 원천 소스에서 더 이상 매칭되지 않아 **3회 미노출 아웃 로직(`Strike-Out`)에 의해 강제 비활성화(`is_active = false`)** 처리됩니다. 이로 인해 상세 정보(raw_data, 카카오 매핑 정보)가 소실될 수 있습니다.\n\n### B. 일일 지역 로테이션 동기화 엔진 (`daily-region-sync.mjs`)\n- **순환 주기 변경*
<truncated 6702 bytes>
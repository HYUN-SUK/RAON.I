# 🔔 알림 시스템 재정비 심층 보고서

## 1. 현재 알림 시스템의 문제점 (Current Problems)
현재 운영 중인 알림 시스템에서 발견된 주요 문제는 다음과 같습니다:
1.  **관리자 알림 누락**: 관리자가 예약 확정/취소를 눌러도 DB에는 기록되지만 실제 푸시가 발송되지 않음 (치명적).
2.  **구조적 불일치**: 매뉴얼은 "DB 트리거 자동 발송"을 원칙으로 하나, 실제로는 트리거가 삭제되어 수동 발송(fetch)에 의존함.
3.  **불안정한 발송 로직**: `camping-reminder` 엣지 함수가 직접 다른 엣지 함수를 호출(`fetch`)하여 트랜잭션이 보장되지 않고 중복 발송 위험이 있음.
4.  **타입 정의 누락**: D-4 알림 중요도가 높음에도 별도 타입 없이 D-1 코드를 재사용하여 통계/관리가 불가능함.

## 2. 매뉴얼 방식(DB Trigger)으로 해결 가능한가? (Feasibility)
**결론: 네, 완벽하게 해결 가능합니다.**
- **누락 해결**: 관리자 페이지든, 스케줄러든 `notifications` 테이블에 데이터를 넣기만 하면 트리거가 즉시 엣지 함수를 깨워 발송하므로 "누락"이 원천 차단됩니다.
- **안정성 확보**: 비즈니스 로직(예약 확정)과 발송 로직(푸시 전송)이 분리되어, 예약을 확정했는데 푸시 서버 에러로 롤백되는 문제를 방지할 수 있습니다.
- **코드 단순화**: `fetch` 호출 코드를 모두 제거하고 순수하게 `INSERT`만 남기면 되므로 유지보수가 쉬워집니다.

## 3. 우리 앱의 필수 알림 시나리오 (Required Scenarios)
앱 서비스 운영을 위해 반드시 필요한 알림 상황은 다음과 같습니다:

| 상황 | 시점 | 중요도 | 대상 | 비고 |
|:---:|:---:|:---:|:---:|:---|
| **예약 신청** | 고객이 입금 대기 상태로 예약 시 | 높음 | 고객 본인 | 입금 계좌/금액/기한 안내 |
| **예약 확정** | 관리자가 입금을 확인했을 때 | **매우 높음** | 고객 본인 | 입실/퇴실/매너타임 안내 |
| **예약 취소** | 관리자가 직권으로 취소했을 때 | **매우 높음** | 고객 본인 | 환불/취소 사유 안내 |
| **빈자리 알림** | 찜한 날짜에 자리가 났을 때 | 높음 | 대기 신청자 | **이미 구현됨 (`waitlist-notifier.ts`)**<br>서비스 수정 시 자동 정상화됨 |
| **D-4 알림** | 입실 4일 전 (장비 점검) | 중간 | 예약자 | 날씨 기반 장비 팁 |
| **D-1 알림** | 입실 1일 전 (메뉴 추천) | 중간 | 예약자 | 날씨/인원 기반 메뉴 추천 |
| **D-0 알림** | 입실 당일 (웰컴) | 중간 | 예약자 | 체크인 안내/지역 행사 정보 |
| 커뮤니티 반응 | 내 글에 댓글/좋아요 | 낮음 | 작성자 | (조용 알림 - 배지만 생성) |

## 4. 현재 구현 상태 (Current Implementation)
현재 코드를 분석한 결과는 다음과 같습니다:

- **예약 확정/취소 (`reservation.ts`)**: `notificationService.dispatchNotification`을 호출하지만, 내부적으로 DB `INSERT`만 하고 트리거가 없어 발송되지 않음.
- **빈자리 알림 (`waitlist-notifier.ts`)**: `notificationService.dispatchNotification`을 정상적으로 호출 중. **서비스 파일만 고치면 즉시 정상 동작**함.
- **캠핑 리마인더 (`camping-reminder/index.ts`)**:
  - D-0 (당일): 구현됨 (`upcoming_stay_today`)
  - D-1 (하루전): 구현됨 (`upcoming_stay_d1`)
  - **D-4 (4일전): 구현됨 (`upcoming_stay_d1` 타입을 재사용 중 - 수정 필요)**
  - **발송 방식**: DB Insert 후 `fetch`로 엣지 함수를 직접 호출하는 "변칙적인 방식" 사용 중.

## 5. 매뉴얼 불일치 분석 (Discrepancy Analysis)

| 항목 | 매뉴얼 기준 (Standard) | 현재 코드 (Current) | 판정 |
|:---:|:---|:---|:---:|
| **발송 메커니즘** | **DB Trigger 자동 감지** | **수동 API 호출 (fetch)** | ❌ **불일치** |
| **DB 저장 상태** | `status: 'queued'` | `status: 'processing'` | ❌ **불일치** |
| **D-4 알림 타입** | `UPCOMING_STAY_D4` | `UPCOMING_STAY_D1` (재사용) | ❌ **불일치** |
| **관리자 알림** | DB Insert → 자동 발송 | DB Insert → (묵묵부답) | ❌ **오동작** |

## 6. 재정비 계획 (Reorganization Plan)

위 분석을 토대로, **매뉴얼 기준에 100% 일치하도록** 시스템을 정상화하겠습니다.

### ✅ Step 1. DB 트리거 복원 (Core Fix)
- 삭제된 `trigger_push_notification`을 다시 생성합니다.
- 조건: `status = 'queued'`인 데이터가 들어오면 `push-notification` 엣지 함수를 실행.

### ✅ Step 2. 알림 서비스(`notificationService.ts`) 표준화
- 알림 생성 시 초기 상태값을 `'processing'`에서 **`'queued'`**로 변경합니다.
- **효과**: 예약 액션(`reservation.ts`)과 **빈자리 알림(`waitlist-notifier.ts`)** 모두 별도 수정 없이 즉시 정상화됩니다.

### ✅ Step 3. 리마인더 엣지 함수(`camping-reminder`) 정상화
- `fetch` 호출 로직을 전면 제거합니다.
- D-4 알림을 위한 전용 로직을 분리하고, 타입을 `UPCOMING_STAY_D4`로 지정합니다.

### ✅ Step 4. 타입 정의 업데이트
- `notificationEvents.ts`에 `UPCOMING_STAY_D4`를 정식 등록하고 템플릿을 설정합니다.

이 계획대로 진행하면, **모든 알림(빈자리 포함)이 DB 중심 아키텍처로 통일**되며 관리자 알림 누락 문제도 자연스럽게 해결됩니다.
승인해주시면 즉시 반영하겠습니다.

### ✅ 엣지 함수 정상화 (Normalized Edge Function)
- **파일**: `supabase/functions/camping-reminder/index.ts`
- **수정**:
  - `push-notification` 함수를 직접 호출하던 `fetch` 코드를 제거했습니다.
  - D-4 알림에 올바른 타입(`UPCOMING_STAY_D4`)과 `'queued'` 상태를 적용했습니다.
- **장점**: 트랜잭션 안전성이 확보되고, 중복 발송 위험이 사라졌습니다. (Fire-and-Forget)

# 세종시 지역 데이터 최적화 및 병원 뱃지 로직 개선 워크스루

이번 작업에서는 세종특별자치시의 특수한 행정 구역 구조로 인해 발생했던 데이터 누락 문제를 해결하고, 병원 데이터의 품질을 높이기 위한 인증 로직을 강화했습니다.

## 1. 문제 분석 및 원인 파악
- **현상**: 국립세종수목원 일정 생성 시 세종충남대병원 등 주요 병원이 누락되고 일반 병원이 상위에 노출됨.
- **원인**: 
  1. 세종시는 '시/군/구' 단계가 없으나, 기존 엔진이 주소의 두 번째 단어(예: 수목원로)를 시군구로 오인하여 NMC API 파라미터로 전달함.
  2. 파싱 실패 시 기본값으로 '예산군'이 들어가도록 하드코딩된 로직이 있어 세종 지역 데이터가 예산 지역으로 조회됨.
  3. NMC API 응급실 정보 수집 시 주소/좌표가 없는 경우 카카오 검색으로 복구하는데, 검색어 구성(`ptSigungu + 명칭`)이 세종시의 경우 공백으로 시작되어 검색 실패 발생.

## 2. 주요 수정 내용
- **주소 파싱 예외 처리**: `extractSigungu` 함수에서 '세종특별자치시'인 경우 빈 문자열(`""`)을 반환하도록 하여 NMC API가 세종시 전체를 조회하게 함.
- **하드코딩 제거**: 클러스터 루프 내의 `|| '예산군'`, `|| '충청남도'`와 같은 레거시 기본값 설정을 제거.
- **NMC 뱃지 자동 부여**: `api_source: 'NMC_HOSPITAL'`인 경우 점수(150점)와 관계없이 '응급의료센터' 뱃지를 강제로 유지하도록 로직 보강.
- **복구 검색어 개선**: 지오코딩 복구 시 시군구가 없으면 시도명을 사용하도록 검색어 구성 로직 수정.

## 3. 검증 결과
- **NMC 수집 정상화**: 세종 지역 병원 수신 건수가 0건에서 5건으로 정상화됨.
- **랭킹 정상화**: 세종충남대학교병원이 150점으로 세종수목원 일정의 병원 리스트 1위에 안착.
- **리포트 확인**: `smart_plan_stage1_full.md` 및 전용 상세 리포트에서 '응급의료센터' 뱃지 출력 확인.

## 4. 관련 파일
- [Stage 1 리포트](file:///c:/Users/USER/Desktop/RAON.I/smart_plan_stage1_full.md)
- [세종수목원 후보군](file:///c:/Users/USER/Desktop/RAON.I/sejong_arboretum_stage1_candidates.md)
- [세종수목원 최종결과](file:///c:/Users/USER/Desktop/RAON.I/sejong_arboretum_stage4_final_plan.md)

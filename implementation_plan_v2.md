# [v2] 스마트 캠핑 개인화 캐싱 파이프라인 구현 계획서

사용자님의 핵심 설계 철학을 반영하여, 롤백된 v11.9.22 기반 위에서 개인화 후보군 주입(Candidate Injection) 기능을 안전하게 결합하는 계획입니다.

## 1. 핵심 아키텍처 (Flow)

1.  **Stage 1 (동적 수집)**: 병원, 주유소, 축제 실시간 데이터를 수집하여 마스터 DB에 안착 (기존 로직 유지).
2.  **Stage 2 (1차 쿼터 선별)**:
    *   클러스터 내 각 기준점(RepPoint)별로 독립적인 품질 점수 계산.
    *   기준점당 **식당 50, 명소 50, 마트 20, 기타 10개**를 우선 선별.
    *   선별된 리스트를 권역 단위로 통합하고 중복을 제거하여 **Union Pool** 생성.
3.  **Stage 3 (카카오 검증)**: 통합된 Union Pool에 대해 Kakao API 실시간 검증 및 상세 데이터 확보.
4.  **Stage 4 (개인화 주입)**:
    *   검증된 풀에서 각 예약자(`reservation_id`)의 위치를 기준으로 **거리 감점(Penalty)** 적용.
    *   최종 **식당 15, 명소 15, 마트 12, 기타 6개**를 선별.
    *   `smart_plan_candidates` 테이블에 최종 안착.

## 2. 세부 구현 사항

### [MODIFY] scripts/caching-smart-plan.mjs
- **품질 스코어링**: Prestige 60/40, LX 맛집 등 기존의 정밀한 점수 산정 로직을 절대 훼손하지 않음.
- **쿼터 로직**: `repPoints` 루프 내부에서 `slice(0, limit)`를 먼저 수행하여 기준점별 대표성을 보장.
- **리포트 기능**: 검증용 `1st_quota_raw_list.md` 및 `2nd_quota_final_list.md` 생성 로직 포함.

## 3. 검증 계획

- **대상**: 2026년 4월 27일 예약 데이터
- **방법**:
    1. 스크립트 실행 후 생성된 1차/2차 리포트의 수량 및 품질 직접 확인.
    2. `smart_plan_candidates` 테이블의 데이터 주입 무결성 확인.

---
> [!IMPORTANT]
> 이 계획은 기존의 검증된 로직을 보존하면서 최 하단에 개인화 레이어만 추가하는 방식입니다.
